# SafarSuite Control Desk Project Tracker

Date started: 2026-06-30

Use this tracker to keep the project slow, deliberate, and on track.

Status values:

- Proposed
- Accepted
- In Progress
- Done
- Deferred

## Current Milestone

| Milestone | Goal | Status |
| --- | --- | --- |
| Milestone 1: Client Maintenance Spine | Maintain SafarSuite clients and only the accounting/billing/cloud support needed for client control | In Progress |

## Latest Checkpoint

| Date | Status | Notes |
| --- | --- | --- |
| 2026-07-05 | Milestone stabilization path started | Added `docs/planning/milestone-stabilization-runbook-2026-07-05.md` and completed the first stabilization pass. App verification passed: LocalServer build, ProductKernelGuardSmoke 40 checks, ReadOnlyEnforcementSmoke 24 checks, ReportExecutionSmoke 30 checks, runtime Compose config, and `git diff --check`. Control Desk verification passed: API build through alternate output, UI production build with the known Vite chunk warning, ProductAccessCatalogSmoke 4 checks, and `git diff --check`. The tracked app `.vs` cache churn was restored and app `.vs` status is clean. Remaining cleanup lanes are separating the Control Desk client-register UI redesign from the platform-spine checkpoint, and one final three-service live golden path when app CloudServer and app LocalServer smoke instances are intentionally running. |
| 2026-07-05 | App runtime health/profile/log diagnostics landed | In `C:\Users\Daniyal\Documents\Codex\2026-06-09\hello-there-2`, the app LocalServer now exposes `safarsuite-local-runtime-profile-v1` through `GET /api/v1/local-server/runtime/profile` and embeds the same runtime profile in `GET /health`. The profile is read from the Control bootstrap/runtime env contract, including app version, deployment mode, site id, site role, optional parent site, branch code, sync topology, local API URL, module gateway URL, and runtime manifest path. Module-gateway decisions now write structured logs with module, allowed state, access state, deployment mode, site id, site role, requested installation id, and local installation id, giving Control-side log-tail diagnostics app-runtime context. App verification passed: LocalServer build, ProductKernelGuardSmoke 40 checks, ReadOnlyEnforcementSmoke 24 checks, ReportExecutionSmoke 30 checks, runtime Compose config, and `git diff --check`. |
| 2026-07-05 | Product access catalog persistence smoke automated | Added `tools\SafarSuite.ControlDesk.ProductAccessCatalogSmoke`. The smoke reads the live Control Desk catalog, saves a temporary owner-managed Payroll group/resource, verifies persisted readback and resolver output, restores the original catalog by default, and can optionally publish/import a signed app product-kernel catalog command when supplied an activation request id and LocalServer URL. Build passed and the default live smoke passed 4 checks against `http://localhost:5188`. |
| 2026-07-04 | Local-server app-runtime diagnostics strengthened | Command-triggered local-server diagnostics now include signed runtime-manifest intent in each service detail: service role, Compose profile, image environment variable, host port environment variable, internal URL, health URL, and dependencies. The optional `safarsuite-app` slot is still correctly reported as `ProfileDisabled` when the app profile is not started, but the diagnostic bundle now proves it is wired to `SAFARSUITE_APP_IMAGE`, `SAFARSUITE_APP_HTTP_PORT`, and `http://safarsuite-app:5280/health`. `tools\SafarSuite.LocalServer.EntitlementSmoke`, the LocalServer infrastructure build, the entitlement-smoke build, and `git diff --check` passed. |
| 2026-07-04 | Control Desk product access catalog owner editing persisted | Added owner-managed product access catalog persistence for Control Desk. `PUT /api/v1/contracts/product-access-catalog` now saves module groups/resources, reads and publish use the saved catalog when present, PostgreSQL stores the catalog in `control.product_access_catalogs` with an in-memory dev/test repository, and the Contracts `Access catalog` view can add/update/remove groups and resources before publishing to the app product kernel. |
| 2026-07-04 | Accounting/GL MVP checkpoint captured | See `docs/planning/accounting-gl-mvp-checkpoint-2026-07-04.md`. Current GL slice is directionally aligned with legacy controlled COA/voucher/report evidence and is verified through build, UI build, accounting smoke, live default-control probe, and diff whitespace check. Baseline includes MAIN-only controlled COA ranges, hierarchy guards, reconciliation/repair-plan review, default GL controls, periods, manual journals, trial balance, profit and loss, balance sheet, period close handoff, and close-journal drill-in. Remaining gaps are opening balances, voucher type/numbering setup, branch/adjusted-date reporting, legacy import dry-run, guided mutating repairs, and field-by-field legacy parity. |
| 2026-07-04 | Control Desk product access catalog publish proved against real app workspace | Live cross-workspace smoke used existing Control Desk API on `http://127.0.0.1:5188`, app CloudServer on `http://127.0.0.1:5281`, and app LocalServer on `http://127.0.0.1:5290`. Control Desk `POST /api/v1/contracts/product-access-catalog/product-kernel-command` requested an app-cloud-signed `SetProductAccessCatalog` command, the app LocalServer imported the activation token and applied the returned command through `/api/product-kernel/vendor-commands`, and `/api/product-kernel/state` readback showed 7 catalog groups and 6 resources including `reporting` and `reports.execute`. App `tests\ProductKernelGuardSmoke` now passed 37 checks, including app CloudServer-issued catalog command import. |
| 2026-07-04 | Real app module-gateway enforcement and runtime wrapper verified | In `C:\Users\Daniyal\Documents\Codex\2026-06-09\hello-there-2`, the app local-server exposes the v1 module-gateway routes, the Windows client folds gateway `isAllowed` into menu/window access, backend `LocalWritePolicy` denies writes when the gateway denies a module, and reporting execution/audit now require `reporting-core` gateway access while still allowing read-only `Restricted` report access. `Dockerfile.localserver`, `.dockerignore`, `docker-compose.runtime.yml`, and `docker/localserver.env.template` now define the first app runtime image/Compose contract. Control Cloud bootstrap templates now point the optional `safarsuite-app` slot at `ghcr.io/danionwheels/localserver`, port `5280`, and `http://safarsuite-app:5280/health`. `tests\ProductKernelGuardSmoke` passed 30 checks, `tests\ReadOnlyEnforcementSmoke` passed 24 checks, `tests\ReportExecutionSmoke` passed 28 checks, Windows client production build passed, local-server Release publish passed, Compose config validation passed, the published LocalServer internal healthcheck passed, Docker image build passed, Compose runtime started healthy on app port `5280` and Postgres host port `55632`, 39 local migrations applied, v1 module-gateway container probes returned the expected contract shape, pushed GHCR image `ghcr.io/danionwheels/localserver:0.1.0` with digest `sha256:0deacfd234d59354d7560371d9b475633903e7d18dd0a84cb5cfbb0cdb182ba1`, anonymous manifest/pull checks passed using an empty Docker config, and the Control Desk local-server entitlement/bootstrap smoke passed. |
| 2026-07-03 | Control spine live proof passed; app runtime probe ready | See `docs/planning/control-spine-checkpoint-2026-07-03.md`, `docs/planning/control-spine-demo-runbook.md`, and `docs/planning/safarsuite-app-integration-handoff.md`; solution build, UI build, accounting smoke, local-server entitlement/command-processing smoke, app runtime probe self-test, live PostgreSQL publishing, bootstrap registration, entitlement pull, heartbeat, diagnostics upload, command acknowledgement, and module-gateway access passed. Command-triggered local-server diagnostics now collect the signed runtime manifest, Docker/Compose availability, live Compose service state, recent warning/error log-tail lines, and the optional `safarsuite-app` profile slot. `tools/SafarSuite.AppRuntimeProbe` is now the placeholder app-side consumer of the module-gateway v1 contract. Next recommended move is the real SafarSuite app workspace module-gateway integration. |

## Scope Rule

SafarSuite Control Desk is a client maintenance system first. Accounting, billing, payments, and cloud publishing stay active only when they support maintaining a client, charging that client, or controlling that client's access. Survey/FAS work is not part of the active product surface.

## Active Work

| Work item | Module | Status | Notes |
| --- | --- | --- | --- |
| Layered architecture contract | Architecture | Done | See `docs/architecture/layered-architecture.md` |
| Canonical product direction | Architecture/Product | Done | See `docs/architecture/product-direction.md`; records the north-star goal, system responsibilities, module-control chain, communication boundaries, non-goals, and drift-check questions for Control Desk, Control Cloud, Client Portal, and SafarSuite |
| Canonical product naming | Architecture | Done | See `docs/architecture/product-naming.md` |
| Cloud/local communication map | Architecture/ControlCloud/LocalServer | Done | See `docs/architecture/cloud-local-communication-map.md`; records Control Desk, Control Cloud, Client Portal, and SafarSuite local-server communication paths for license, heartbeat, command, and portal alignment |
| Client deployment modes and data-sync boundary | Architecture/Product/ControlCloud/LocalServer | Done | See `docs/architecture/client-deployment-and-data-sync-boundary.md`; SafarSuite supports offline local, branch-to-HQ sync, cloud-sync multi-branch, and hosted SaaS modes while Control Cloud remains the commercial/control channel and operational branch/HQ/cloud business-data sync stays in a separate future data plane |
| Product module catalog boundary | Contracts/Entitlements/Billing/Frontend/Architecture | Basic done | See `docs/architecture/product-module-catalog-boundary.md`; records the flexible IncludedForAll/PaidAddOn module catalog, seeds temporary dummy modules with billing defaults, enforces active-catalog module selection during contract create/replace, lets the contract UI use catalog checkboxes, lets billing setup prefill charge-code/rule forms from active paid add-ons, and persists the originating module code on saved charge rules and generated invoice draft lines |
| Legacy source reference | Legacy | Done | See `docs/legacy/source-reference.md` |
| Travel/Champion accounting sweep | Legacy/Accounting/Travel | Accepted | See `docs/planning/travel-legacy-sweep.md` and `docs/planning/accounting-gl-foundation-sweep.md`; records `TRV.mdb`, `TRV.mde`, and `Data.sql` evidence for Control Desk accounting direction: controlled COA, party ledger identities, voucher/journal posting, invoice/receipt/refund chains, tax/discount setup, branch/currency context, and later Travel/Tour module parity |
| Backend solution scaffold | Platform | Done | Created solution and projects for Api, Application, Domain, Infrastructure, Contracts |
| Frontend scaffold | Platform | Done | Vite React TypeScript app created under `apps/control-desk-ui` |
| Client maintenance UI | Clients/Frontend | Done | Active frontend now opens the client desk with list, create, detail, edit, lifecycle, support notes, and accounting-profile status |
| First domain model draft | Clients/Contracts/Billing/Payments/Entitlements/Audit | Done | Core aggregate roots and repository ports created |
| Client maintenance basics | Clients | Done | Client detail, edit, activate, and suspend API actions are wired |
| Client contacts | Clients | Done | Add/list structured contacts with roles and primary flag; active UI shows contacts |
| Client support notes | Clients | Done | Add/list internal client notes; client detail includes note history |
| PostgreSQL local database foundation | Persistence | Done | Docker Compose, local EF tool, EF DbContext, and initial migration are in place |
| Client maintenance persistence | Clients/Persistence | Done | PostgreSQL slice persists clients, contacts, and support notes |
| Client contract persistence | Contracts/Persistence | Done | PostgreSQL slice persists active client contracts and module allowances |
| Client contract maintenance | Contracts/Frontend | Basic done | Client desk can list contracts, create a contract, replace the active contract, and suspend active contracts |
| Accounting persistence | Accounting/Persistence | Done | PostgreSQL slice persists ledger accounts, journal entries, and journal lines |
| Billing/Profile/Outbox persistence | Billing/Clients/ControlCloud/Persistence | Done | PostgreSQL slice persists client accounting profiles, charge codes, client charge rules, invoices, invoice lines, and cloud outbox messages |
| Payment persistence | Payments/Persistence | Done | PostgreSQL slice persists invoice payments, including pending-review bank transfers, approved receipts, invoice balance/status updates, and receipt journals |
| Client billing setup UI | Billing/Frontend | Basic done | Client desk can maintain accounting profile, charge/tax setup, invoice draft, and invoice issue flow; ledger account setup now shows enforced GL defaults, uses `GL_Working.xlsx`-aligned controlled numeric account-code suggestions, and auto-links the AR account to the client profile |
| Client payment and entitlement UI | Payments/Entitlements/Frontend | Basic done | Client desk can record invoice payments, review bank transfers, reverse approved payments, apply/refund client credit balances, and issue/refresh local entitlement snapshots |
| Client statement/receivables UI | Clients/Billing/Accounting/Frontend | Basic done | Client desk can show invoices, payments, credit notes, applied credit, client refunds, available credit, running receivable balance, and related journal postings |
| Accounting/GL correction sweep | Accounting/Billing | Done | Invoice creation split clarified in `accounting-gl-foundation-sweep.md` |
| Controlled COA setup backlog | Accounting/Billing/Frontend | GL MVP checkpoint done | `docs/planning/accounting-gl-foundation-sweep.md` tracks the `GL_Working.xlsx`-aligned backlog and `docs/planning/accounting-gl-mvp-checkpoint-2026-07-04.md` freezes the current baseline. Accounting Setup persists company-scoped ranges, role mappings drive suggestions, ledger account creation is range/type/balance/posting/level guarded, Header/Total ranges exist for major classes, Subsidiary creation requires a Control parent, the Accounting workspace can maintain the COA/register/ranges, and reconciliation plus repair-plan review remain read-only. Remaining work is legacy import dry-run, opening balances, and deliberate guided repair actions. |
| Accounting/Billing foundation | Accounting/Billing | In Progress | Dynamic charge/tax setup, invoice draft generation, profile-assisted invoice issue GL posting, unpaid invoice voiding, full credit notes, client refunds, credit settlement, payment review/reversal, signed local outbox publishing, journal/ledger read models, client statement visibility, full-chain accounting smoke coverage, UI guardrails, GL reports, period guards, default GL controls, period close handoff, close-journal drill-in, the first Control Cloud receiver boundary, PostgreSQL-backed cloud commercial projection, installation-bound cloud entitlement bundle issue audit, signed installation command acknowledgements, local-server setup-token registration, local-server entitlement pull/verification/cache/feature gates, paid-module guardrails, heartbeat/status, Control Desk cloud status panel, portal preview, portal invite/session audit, offline renewal file fallback, local clock/replay trust state, local import audit persistence, bootstrap generation/templates, runtime service manifest boundary, setup/bootstrap/registration audit visibility, diagnostics export/upload, runnable local-server API host, product catalog boundary, real app workspace module-gateway enforcement, and app runtime health/profile/log diagnostics are wired; portal payment UI, production install proof for the app profile, catalog management screens, opening balances, voucher numbering/type setup, branch-aware reporting, and legacy report parity are still pending |
| Client accounting profile | Clients/Accounting | Done | Client can be linked to AR/default currency/cloud identity; invoice issue can resolve AR from the profile, and the client desk no longer requires manually pasting the AR ledger account id during normal setup |
| Cloud invoice outbox | Billing/ControlCloud | Done for local loop | Invoice issue creates persisted `InvoiceIssued` messages, and the local publisher can mark signed pending outbox messages sent |
| Payment outbox events | Payments/ControlCloud | Done | Approved receipt posting enqueues persisted `PaymentRecorded` and `ClientPaidStatusChanged`; reversals enqueue `PaymentReversed` and reopen paid-status when needed |
| Local outbox publisher | ControlCloud | Done | Manual dev endpoint builds signed envelopes and marks ready outbox messages sent/failed without calling SafarSuite Control Cloud |
| Control Cloud publish readiness | ControlCloud | Basic done | Signed payload envelope, local/HTTP publisher adapter, retry attempt metadata, and config-driven publish mode are wired |
| Control Desk to Control Cloud local publish | ControlCloud/Integration | Basic done | Control Desk Development config points HTTP publishing to the local Control Cloud receiver, and the accounting smoke can publish seven real outbox rows through the receiver |
| Client entitlement chain | Entitlements/ControlCloud/LocalServer | In Progress | Paid invoices can now issue persisted local entitlement snapshots from active contract defaults and enqueue signed `EntitlementSnapshotIssued` publish messages; cloud-side commercial projection accepts entitlement snapshots, registers installations through one-time setup tokens, issues installation-bound audited HMAC-signed portal/local-server entitlement bundles, exports offline renewal files, queues signed installation commands with acknowledgement audit, and local-server libraries can register, pull, import, cache, gate, reject replay, record local import audit, record clock warnings, and report heartbeat/license state |
| Offline entitlement control rules | Entitlements/ControlCloud/Client Portal | Accepted | `docs/planning/offline-entitlement-control-rules.md` defines paid-period offline validity, heartbeat separation, warning/grace/restriction states, offline renewal files, trust-based lease lengths, command acknowledgements, replay protection, and audit requirements |
| Control Cloud deployment tracker | ControlCloud/Client Portal/Local Server | Accepted | `docs/planning/control-cloud-deployment-tracker.md` defines the one-cloud rule, portal-controlled/local-server-pulled deployment, V1 Docker Compose bootstrap bundle, and later `.deb` packaging path |
| Control Cloud receiver skeleton | ControlCloud/API/Application/Infrastructure | Basic done | `SafarSuite.ControlCloud.Api` receives signed Control Desk envelopes, validates payload hash/HMAC, records accepted/rejected/duplicate receipts in PostgreSQL, projects accepted commercial messages into a portal-readable read model, and returns stable cloud message references |
| Control Cloud PostgreSQL persistence | ControlCloud/Persistence | Done | Cloud receiver receipts and client commercial projections are persisted in PostgreSQL under the `cloud` schema with a filtered unique accepted-idempotency index |
| Control Cloud portal commercial projection | ControlCloud/ClientPortal | Basic done | Accepted invoice, payment, credit note, refund, credit-application, paid-status, and entitlement-snapshot envelopes update PostgreSQL-backed `GET /api/v1/client-portal/clients/{clientId}/commercial-summary` |
| Client Portal identity/session boundary | ControlDesk/ControlCloud/ClientPortal | Basic invite management foundation done | `docs/architecture/client-portal-identity-boundary.md` records that Control Cloud owns portal credentials; Control Desk can request, list, resend, and revoke portal invites for client contacts, Control Cloud protects invitation management with a provider key, invitation delivery is pluggable between local file and SMTP, invite/session audit records are written, and invite acceptance creates password-backed client-scoped sessions; real provider users, MFA, password reset, and production mail retry handling remain pending |
| Control Cloud entitlement signing boundary | ControlCloud/ClientPortal/LocalServer/Entitlements | Basic done | Latest projected entitlement snapshots can be returned as installation-bound HMAC-signed bundles through the session-protected Client Portal route, the machine-facing `GET /api/v1/local-server/installations/{installationId}/entitlement-bundle?clientId={clientId}` route, and the offline renewal file export route; bundle issues and installation registry state are persisted in PostgreSQL |
| Control Cloud installation command queue | ControlCloud/LocalServer/Entitlements | Basic done | Control Cloud can queue signed monotonic commands for registered installations, local servers can pull pending commands and acknowledge Applied/Failed/Rejected results, PostgreSQL persists command plus acknowledgement audit rows, and command signatures are stable across `jsonb` payload reordering plus microsecond timestamp precision round-trips |
| Control Cloud installation heartbeat | ControlCloud/LocalServer | Basic done | Local servers can report heartbeat to `POST /api/v1/local-server/installations/{installationId}/heartbeat`; Control Cloud stores heartbeat status separately from reported license state in PostgreSQL/file persistence |
| Control Cloud installation status view | ControlCloud/ClientPortal/ControlDesk | Basic portal preview done | Shared status is available through Control Cloud and Client Portal routes; the Control Desk client page can refresh by installation id, and `/client-portal/index.html` can show commercial, license, heartbeat, entitlement, and command status for a client/installation |
| Control Cloud setup-token registration | ControlCloud/LocalServer | Basic done | Control Cloud can create one-time hashed setup tokens for client installations, local servers can register outward with token scope/expiry checks, and entitlement issue now requires a registered installation |
| Control Cloud bootstrap package generation | ControlCloud/LocalServer | Basic done | Control Cloud can generate a setup-token-backed bootstrap package with cloud registration URLs, entitlement/heartbeat/command/diagnostics endpoints, a copyable install command, signed JSON bootstrap bundle download, artifact checksums, first served `install.sh`, Docker Compose template, environment template, runtime services manifest, and optional SafarSuite app profile slot for later SafarSuite app/runtime consumption |
| Control Desk installation provisioning controls | ControlDesk/ControlCloud/Frontend | Basic done | Client desk Cloud tab can save the local deployment profile, create a Control Cloud setup token, create a bootstrap package/install command, and show generated token/package details through a layered Control Desk provisioning proxy |
| Control Desk installation history visibility | ControlDesk/ControlCloud/Frontend/Audit | Basic done | Client desk Cloud tab can refresh recent setup/bootstrap/registration/diagnostics/renewal audit events for the selected installation through a layered Control Desk audit proxy |
| Control Desk installation diagnostics visibility | ControlDesk/ControlCloud/Frontend/Diagnostics | Basic done | Client desk Cloud tab can refresh and download the latest diagnostics report for the selected installation through a layered Control Desk diagnostics proxy |
| Control Desk support command queue controls | ControlDesk/ControlCloud/Frontend/LocalServer | Basic done | Client desk Cloud tab can queue whitelisted `request_diagnostics` and `refresh_entitlement` support commands through a layered Control Desk proxy into Control Cloud's signed installation command queue; local-server command execution now processes both command types and acknowledges outcomes |
| SafarSuite runtime integration boundary | ControlCloud/LocalServer/SafarSuiteApp | App runtime diagnostics verified | Bootstrap packages now include a signed runtime plan, diagnostics endpoint, `runtime-services.manifest.json`, optional `safarsuite-app` Compose profile slot, shared module-gateway contract, runnable local-server API host, runtime diagnostics probing/log tail, service manifest-intent details, the placeholder app runtime probe in `tools/SafarSuite.AppRuntimeProbe`, and the app-workspace handoff at `docs/planning/safarsuite-app-integration-handoff.md`; the real app workspace now verifies v1 module-gateway menu/window access, backend write enforcement, reporting execution/audit enforcement, Windows client build, local-server publish, runtime Compose config, internal healthcheck, pushed image `ghcr.io/danionwheels/localserver:0.1.0`, runtime profile endpoint, profile-enriched health response, and module-gateway logs with deployment/site context; the Control Cloud bootstrap app slot now matches the verified pushed app image/port/health/profile env contract; production install proof for the app profile remains pending |
| Local module gateway contract | LocalServer/SafarSuiteApp/Entitlements | App workspace verified | Shared `LocalServerModuleAccessRequest`/`LocalServerModuleAccessResponse` contracts, local API endpoints, app workspace GET/POST v1 routes, app client access-policy consumption, and backend write-policy enforcement expose module access evaluation through the local module gateway |
| Local server API host | LocalServer/ControlCloud/SafarSuiteApp | Basic done | `SafarSuite.LocalServer.Api` boots with bootstrap import/status, entitlement pull, heartbeat, command processing, module access endpoints, file-backed local stores, and a disabled-by-default background pull/heartbeat/command worker |
| Deployment topology vocabulary | Contracts/ControlCloud/LocalServer | Basic done | Shared constants now distinguish bootstrap modes (`OnlineBootstrap`, `OfflineAssistedBootstrap`), client deployment modes (`OfflineLocal`, `BranchToHqSync`, `CloudSyncMultiBranch`, `HostedSaas`), and control versus operational data planes |
| Installation/deployment profile | ControlCloud/LocalServer/Contracts/Diagnostics | Basic done | Setup tokens and registered installations persist bootstrap mode, client deployment mode, site id, site role, optional parent site, branch code, and sync topology id; bootstrap package, signed bundle, registration, status, heartbeat, diagnostics, install command, and env template surfaces can carry the profile |
| Control Cloud setup/bootstrap audit visibility | ControlCloud/Audit | Basic done | Setup-token creation, bootstrap package generation, and local-server registration accept/reject events are recorded and exposed through `GET /api/v1/control-cloud/audit-events` |
| Local server diagnostics export/upload | LocalServer/ControlCloud/Audit | Runtime manifest intent added | Local-server libraries can export cached entitlement/trust-state diagnostics plus local import audit, runtime/build/channel, Docker/Compose, bootstrap checksum/status, service-state, and recent-error facts, upload to `POST /api/v1/local-server/installations/{installationId}/diagnostics`, and Control Cloud persists/latest-reads diagnostic reports; command-triggered diagnostics now fill runtime/bootstrap/service facts from the signed bootstrap runtime manifest, probe Docker/Compose service state, include each service's role/profile/image-env/port-env/internal-url/health-url/dependency intent, and collect recent warning/error log-tail lines when callers do not provide explicit facts |
| Paid module composition | Contracts/Entitlements/LocalServer/Billing | Basic catalog boundary done | Client contracts and entitlement snapshots require at least one enabled module; a flexible product module catalog can identify IncludedForAll versus PaidAddOn modules; module-backed billing rules and invoice draft lines retain the module code; deployed app access remains controlled by enabled module entries in the signed entitlement bundle |
| Local server entitlement verification | LocalServer/Entitlements | Basic done | New `SafarSuite.LocalServer` layers can pull the latest signed bundle from Control Cloud, import offline renewal files that wrap signed bundles, verify HMAC signatures, reject bad signatures, older versions, and same-file replay, persist trust state for last accepted bundle/last trusted cloud time/clock rollback warnings, persist local import audit records, cache the latest accepted bundle, gate module access through active/warning/grace/restricted/expired states, and report heartbeat/license status |
| Local server import audit | LocalServer/Entitlements/Diagnostics | Basic done | Accepted/rejected Control Cloud pulls, direct bundle imports, and offline renewal-file imports are stored locally with source, result, version, bundle issue, failure code, payload hash, and signing key id; diagnostics includes the recent records |
| Payment review and reversal foundation | Payments/Accounting/Frontend | Basic done | Bank transfers enter pending review without GL posting; approval posts receipt journal and payment events; rejection closes pending review; reversal posts a dedicated reversal journal and updates the client statement |
| Billing tax foundation | Billing/Accounting/Frontend/Persistence | Basic done | Client charge rules support tax percent, invoice drafts materialize tax lines, invoice issue credits tax payable, and EF migration adds tax/line-type persistence |
| Unpaid invoice void foundation | Billing/Accounting/ControlCloud/Frontend | Basic done | Unpaid issued invoices can be voided with a reversing GL journal, `InvoiceVoided` outbox event, and statement visibility; paid invoice correction remains a later credit/refund workflow |
| Full credit note foundation | Billing/Accounting/ControlCloud/Frontend/Persistence | Basic done | Paid and partially paid invoices can receive one full credit note with reversing sale journal, `CreditNoteIssued` outbox event, persistence, and statement credit visibility |
| Client refund foundation | Payments/Accounting/ControlCloud/Frontend/Persistence | Basic done | Client credit balances can be refunded with a balanced AR/cash-bank journal, `ClientRefundIssued` outbox event, persistence, and statement visibility |
| Client credit settlement foundation | Payments/Billing/ControlCloud/Frontend/Persistence | Basic done | Unapplied client credit can be allocated to issued/partially paid invoices without a new GL journal, with `ClientCreditApplied` outbox event, persistence, invoice balance updates, and statement visibility |
| Accounting action UI guardrails | Billing/Payments/Frontend | Basic done | Client desk disables incomplete invoice/payment/credit/refund actions and asks for confirmation before GL-impacting issue, void, credit note, receipt, approval, reversal, settlement, and refund actions |
| Accounting chain smoke runner | Tools/Accounting | GL close handoff done | `tools/SafarSuite.ControlDesk.AccountingSmoke` exercises the core local chain through application handlers and verifies controlled COA setup, repair-plan zero-action checks for clean setup, invalid ledger-code refusals, posting/parent guard refusals, balanced journals, statement credit behavior, outbox events, default accounting controls, reports, and retained-earnings close handoff; both in-memory and PostgreSQL modes have passed historically, with the latest in-memory run passing on 2026-07-04 |

## Parking Lot

| Item | Reason Parked | Status |
| --- | --- | --- |
| Full Survey/FAS form cloning | Too broad for Milestone 1 | Deferred |
| Control Desk UI bundle code-splitting | Vite production build passes, but the main JS chunk is over the default 500 kB warning threshold; split heavy module areas later when frontend flow stabilizes | Proposed |
| SurveyValuation module | No longer part of the product goal for SafarSuite Control Desk; code/docs remain as reference but API routes and DI registrations are not active | Deferred |
| Legacy form clone plan | Historical reference only after pivot away from Survey/FAS clone | Deferred |
| Legacy form name map | Historical reference only after pivot away from Survey/FAS clone | Deferred |
| Survey Job Entry implementation | Work exists but is paused; do not extend unless requirements change | Deferred |
| Tauri desktop packaging | Wait until browser-hosted React UI proves the first flow | Deferred |
| Payment gateway selection | Needs business decision | Proposed |
| Client business-data sync data plane | Future multi-branch/HQ/cloud operational sync beyond the current commercial control, entitlement, heartbeat, diagnostics, and deployment work | Proposed |
| SQL backup restore | Useful later for sample data and parity checks | Proposed |
| PostgreSQL persistence for Survey Job Entry | Not needed while SurveyValuation remains parked | Deferred |
| Broader Survey reference lookups | Not needed while SurveyValuation remains parked | Deferred |
