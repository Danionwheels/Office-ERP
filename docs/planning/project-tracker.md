# SafarSuite Control Desk Project Tracker

Date started: 2026-06-30

Use this tracker to keep the project slow, deliberate, and on track.

Status values:

- Proposed
- Accepted
- In Progress
- Done
- Deferred

## Current Milestone

| Milestone | Goal | Status |
| --- | --- | --- |
| Milestone 1: Client Maintenance Spine | Maintain SafarSuite clients and only the accounting/billing/cloud support needed for client control | In Progress |

## Latest Checkpoint

| Date | Status | Notes |
| --- | --- | --- |
| 2026-07-09 | Client Portal pairing status visibility wired | `/client-portal/index.html` now shows device pairing state from the shared installation status response, including pairing mode, first-manager approval, approved/pending/revoked device counts, and last device update. `tools\SafarSuite.ControlCloud.PostgresProof` now creates and accepts a Client Portal invitation, reads the portal installation status route with the accepted portal bearer token, and asserts the pairing snapshot. Verification passed: client portal JS syntax check, solution build, and ControlCloud.PostgresProof against Docker/PostgreSQL. |
| 2026-07-09 | Control Desk pairing status visibility wired | LocalServer heartbeat now reports a non-secret pairing snapshot with pairing mode, device counts, first-manager approval state, and last device update. Control Cloud stores the snapshot on installation heartbeats in file/PostgreSQL modes with EF migration `AddInstallationHeartbeatPairingStatus`, echoes it from heartbeat and installation status responses, and the Control Desk Cloud tab shows pairing status on the control board, customer setup checklist, and status facts. Verification passed: solution build, Control Desk UI production build with the known large-chunk Vite warning, LocalServer entitlement smoke, ControlCloud.PostgresProof against Docker with migration/status assertions, LocalServer API Release publish, rebuilt `safarsuite-local-server:codex-smoke`, and full Docker Compose runtime proof. |
| 2026-07-09 | LocalServer manager sessions and signed device credentials wired | Added HMAC-signed LocalServer device credentials, `POST /api/v1/local-server/device-credentials/verify`, local manager-session signing/config, `POST /api/v1/local-server/pairing/manager-sessions`, and manager bearer-session protection for device list, pending list, approve, suspend, and revoke routes. First-manager import and manager approval now return signed compact device credentials, manager session actors replace untrusted request-body actors for device-management audit fields, generated runtime env/install artifacts carry the device credential lifetime, and ComposeBootstrapProof now asserts anonymous denial plus authenticated manager-session approval flow. Verification passed: LocalServer API build, Control Cloud API build, Control Desk API build, ComposeBootstrapProof build, ControlCloud.PostgresProof build, ControlDesk.ProviderAccessProxyProof build, LocalServer entitlement smoke build/run with `firstManagerDeviceCredentialVerified: true`, LocalServer Release publish, rebuilt `safarsuite-local-server:codex-smoke` from the current publish output, and full Docker Compose runtime proof on `http://127.0.0.1:18095` using proof stub port `5199` with `HttpOnly` local API mode. |
| 2026-07-09 | Control Cloud first-manager setup-token issue/download wired | Added Control Cloud first-manager setup-token issue contracts, HMAC signing through the bootstrap trust key lane, issue metadata persistence, provider-gated `POST /api/v1/control-cloud/clients/{clientId}/installations/{installationId}/first-manager-setup-token`, Control Desk proxy/application/API wiring, and Cloud tab issue/download controls for the signed token JSON. The Control Desk provider-access proxy proof now seeds a client projection, creates/registers a bootstrap package, issues the first-manager token through Control Desk, and asserts pending-device/signature binding; ControlCloud.PostgresProof and LocalServer EntitlementSmoke also cover the cloud issuer path. Verification passed: Control Cloud API build, Control Desk API build, ControlCloud.PostgresProof build, Control Desk provider-access proxy proof build/run, LocalServer entitlement smoke build/run, and Control Desk UI production build with the existing large-chunk Vite warning. |
| 2026-07-09 | LocalServer first-manager setup-token import wired | Added the shared signed first-manager setup-token contracts, LocalServer HMAC verifier, consumed-token ledger, and `POST /api/v1/local-server/pairing/first-manager-token/import`. The import validates token signature, client/install binding, expiry, pending device binding, replay state, and allowed actions before atomically recording token consumption and auto-approving the first device with a one-time proof credential. ComposeBootstrapProof now exercises wrong-client rejection, wrong-installation rejection, first-manager import, replay rejection, and the regular pending -> approved -> suspended -> revoked lifecycle; EntitlementSmoke verifies valid, tampered, expired, missing, consumed, and approved-device states. Verification passed: LocalServer.Api build, ComposeBootstrapProof build, LocalServer entitlement smoke, LocalServer API Release publish, rebuilt `safarsuite-local-server:codex-smoke`, and full Docker Compose runtime proof. |
| 2026-07-09 | LocalServer device pairing lifecycle wired | Added the file-backed LocalServer device pairing ledger plus endpoints to create pending pairing requests, read request status, list all/pending devices, approve a device with one-time credential issuance, suspend an approved device, and revoke a device. The generated runtime now persists the pairing ledger under the LocalServer state directory, and ComposeBootstrapProof validates pending -> approved -> suspended -> revoked after discovery/hello. Verification passed: LocalServer.Api build, ComposeBootstrapProof build, LocalServer entitlement/runtime smoke, direct LocalServer API HTTP smoke with stub Control Cloud, and full Docker Compose runtime proof after rebuilding `safarsuite-local-server:codex-smoke` from the current LocalServer API publish output. The compose proof passed bootstrap import, entitlement pull, heartbeat, module access, discovery/hello, first-manager device approval, pending pairing request, approval credential issuance, suspend, and revoke. |
| 2026-07-09 | LocalServer pairing hello wired | Added shared SafarSuite app to LocalServer pairing contracts, LocalServer pairing options, public non-secret discovery at `/.well-known/safarsuite-local-server`, bootstrap-bound pairing hello at `POST /api/v1/local-server/pairing/hello`, generated env pairing placeholders, and Compose bootstrap proof assertions for discovery/hello identity. Verification passed: ControlDesk.Contracts build, LocalServer.Api build, ComposeBootstrapProof build with shared compilation disabled, and LocalServer entitlement/runtime smoke. |
| 2026-07-09 | SafarSuite app pairing flow designed | Added `docs/planning/safarsuite-app-local-server-pairing-flow.md` to define LAN/manual/cloud-assisted/offline-assisted discovery, signed trust validation, one-time provider activation versus local device pairing, first-manager setup-token consumption, manager-controlled device/user rights, durable pairing profiles, reconnect behavior, draft endpoint contracts, and the implementation slices needed to avoid repeated client-side setup. Verification passed: `git diff --check` with line-ending warnings only. |
| 2026-07-09 | Customer setup Windows handoff guarded | Hardened the downloaded Control Desk customer setup guide so Windows operators are told to run the installer command from Git Bash or WSL, not directly in PowerShell or Command Prompt. The guide now includes PowerShell launch commands for Git Bash/WSL and a secure handoff note to regenerate the bootstrap package if the guide or signed bundle is sent to the wrong destination. Verification passed: Control Desk UI production build with the known large-chunk Vite warning, and `git diff --check` with line-ending warnings only. |
| 2026-07-09 | Control Desk customer setup checklist wired | Added an evidence-backed Customer setup checklist to the Control Desk Cloud tab. The checklist summarizes deployment profile saved, setup packet ready, local server registered, heartbeat received, entitlement pulled from heartbeat evidence, diagnostics received, and SafarSuite app activation present. It also preserves the setup-packet handoff controls for copying the install command, downloading the signed bundle, downloading the customer guide, and downloading generated runtime artifacts. Verification passed: Control Desk UI production build with the known large-chunk Vite warning. |
| 2026-07-09 | Control Desk customer setup packet started | Moved the first clean-machine proof handoff into the Control Desk Cloud tab. A newly generated bootstrap package now exposes copyable install command actions, signed bundle download, customer setup guide download, and per-artifact runtime downloads for the generated Docker Compose file, env template, and runtime manifest. The setup guide captures package ids, bundle hash, versions, install command, verification sequence, and artifact checksums without changing the backend package contract. Verification passed: Control Desk UI production build with the known large-chunk Vite warning, and `git diff --check` with line-ending warnings only. Remaining UX work is a guided setup checklist across registration, entitlement, heartbeat, diagnostics, and app activation. |
| 2026-07-09 | Control Desk deployment package register UX wired | Added the manager-facing Control Desk UI slice on top of the Control Cloud bootstrap package register. The Cloud tab now fetches the package register, refreshes it from the main cloud refresh and after setup/bootstrap package actions, clears stale rows when the selected deployment changes, and shows a metadata-only deployment package list with package/setup ids, status, generated/expiry/consumption timestamps, runtime/app versions, site, and bundle hash without exposing setup-token plaintext. Provider access scope management now exposes `deployment-packages:read` and `deployment-packages:write`, with read included in the default operator creation template. Verification passed: Control Desk UI production build with the known large-chunk Vite warning, and `git diff --check` with line-ending warnings only. |
| 2026-07-08 | Deployment package register backend wired | Added the first Control Cloud/Control Desk bootstrap package register slice. Control Cloud now stamps non-secret package metadata onto setup-token records, persists it in file/PostgreSQL modes with EF migration `AddBootstrapPackageRegisterMetadata`, exposes provider-gated `GET /api/v1/control-cloud/clients/{clientId}/installations/{installationId}/bootstrap-packages`, gates setup-token/bootstrap-package creation and bundle download behind `deployment-packages:write`, and adds `deployment-packages:read/write` provider scopes. Control Desk now proxies the register read through its own matching endpoint so the next manager UX can list generated packages, statuses, versions, expiry, consumption, bundle file names, and hashes without storing reusable setup-token plaintext. Verification passed: Control Cloud API build, Control Desk API build, ComposeBootstrapProof build, ControlCloud.PostgresProof build, LocalServer entitlement smoke build/run, and `git diff --check` with line-ending warnings only. Next step is the Control Desk customer setup UX panel on top of this register. |
| 2026-07-08 | Clean-machine live deployment proof passed | Ran the real Control Cloud bootstrap package on a clean Docker target with app-runtime enabled. The proof passed LocalServer install, registration, entitlement pull, heartbeat, command processing, diagnostics upload, app activation, module access, and rerun secret-hash reuse; final evidence used Local API `https://127.0.0.1:19080`, app runtime `http://127.0.0.1:19081`, entitlement version `639190761734034536`, diagnostics report `d93de61f-223d-4de5-b185-61e1f7af1b42`, and app activation signing key `clean-proof-app-activation-2026-07`. Hardened the installer/runtime image around CRLF shell shims, CA-pinned Windows curl compatibility, LocalServer trust keys, PEM trust-anchor loading, and idempotent bootstrap import. Next product slice is moving the proved setup steps from generated proof scripts into the Control Desk customer setup UX. |
| 2026-07-08 | Clean-machine proof scripts generated | Made `ComposeBootstrapProof generate-real-cloud` emit clean-target helper scripts beside the real Control Cloud package: `clean-machine-install.sh`, `clean-machine-verify.sh`, and `clean-machine-rerun-secret-proof.sh`. The generated installer script embeds the package client/install/setup token values, app-runtime profile, app activation verifier metadata, CA-pinned Local API settings, and bootstrap bundle checksum; the verify script exercises health, entitlement pull, heartbeat, command processing, and module access; and the rerun script captures/diffs generated secret hashes. The real-cloud bundle file now uses the compact serialization that matches the package checksum. Verification passed: `ComposeBootstrapProof` build with shared compilation disabled and `ComposeBootstrapProof generate` with absolute repo root/output. Remaining work is to run these scripts on the clean Docker/VM target, queue/process diagnostics, activate the app runtime, and record the final evidence. |
| 2026-07-08 | Clean-machine proof environment started | Started the clean-machine deployment proof by defining the PostgreSQL-backed Control Cloud plus clean Docker/VM target environment in `docs/planning/clean-machine-deployment-proof-todo-2026-07-08.md`. Control Cloud can now hydrate receiver HMAC keys, entitlement/bootstrap/command HMAC keys, and app activation ECDSA PEMs from secret files; the compose proof runner accepts file-backed proof credentials; and provider-operator login now has temporary lockout protection after repeated credential/MFA failures with manager-visible lockout state. Verification passed: Control Cloud API build, Control Desk API build, Control Desk UI production build with the known large-chunk warning, ComposeBootstrapProof build, `ProviderAccessSmoke` passed 17 checks, and `ComposeBootstrapProof generate` with file-backed proof secrets. Remaining work is to run the generated installer on the clean target, process diagnostics, activate the app runtime, and capture rerun secret-hash evidence. |
| 2026-07-08 | Bootstrap package carries app activation verifier key | Made Control Cloud bootstrap packages carry the active SafarSuite app activation verifier key metadata in the package response, signed bootstrap payload, generated env artifact, and copyable install command. The real-cloud compose proof now consumes that signed package metadata instead of making a separate signing-key lookup. Verification passed: Control Cloud API build, compose proof build, `SafarSuite.LocalServer.EntitlementSmoke`, `ComposeBootstrapProof generate` with explicit repo root/output, generated artifact inspection, and `git diff --check` with line-ending warnings only. Remaining production work is external secret-manager provisioning automation, app activation key rotation/runbooks, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Local app-runtime deployment secrets made rerun-stable | Hardened the generated LocalServer/app-runtime deployment contract so the installer writes durable generated secrets under the local state directory instead of rotating them on every rerun. `install.sh` now reuses generated Local API access key, local DB password, app device signing secret, and app session signing secret; generated/static env templates expose `DeploymentSecrets__Provider`, app activation signing-key placeholders, device/session signing keys, and first-manager fallback disablement. Verification passed: Control Cloud API build, `SafarSuite.LocalServer.EntitlementSmoke` build/run, `ComposeBootstrapProof generate` with absolute output, and generated env inspection under `artifacts/codex/localserver-deployment-secret-custody`. Remaining production work is carrying the app activation signing key directly in the bootstrap package, external secret-manager provisioning automation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Provider TOTP secret custody protected | Added encrypted custody for stored provider-operator TOTP secrets. Control Cloud now protects TOTP payloads with `ClientPortal:ProviderAccess:TotpProtectionSecret`/`TotpProtectionSecretFile`, stores protected material in file/PostgreSQL operator stores, keeps reset responses one-time plaintext for authenticator enrollment, and migrates legacy plaintext TOTP rows to protected payloads after successful login. Verification passed: Control Cloud API build, provider-access smoke build/run with 14 checks, and Control Desk provider-access proxy proof build/run with 19 checks including protected TOTP custody in PostgreSQL. Remaining production work is WebAuthn/passkey support, external secret-manager provisioning automation, TOTP protection-secret rotation workflow, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Provider TOTP MFA wired | Added TOTP enrollment and login verification for provider operators. Control Cloud now persists TOTP enrollment metadata in file/PostgreSQL operator stores, exposes manager reset under `/api/v1/provider-access/operators/{userId}/totp`, returns one-time secret/otpauth enrollment material, accepts `totpCode` or an unused recovery code during operator session creation, and rejects replayed TOTP steps. Control Desk now proxies TOTP reset, carries optional TOTP codes during operator sign-in, and shows one-time enrollment material in the provider access panel. Verification passed: Control Cloud API build, Control Desk API build, provider-access smoke build/run with 13 checks, Control Desk provider-access proxy proof build/run with 19 checks, and Control Desk UI production build with the known large-chunk Vite warning. Remaining production work is encrypted custody for stored TOTP secrets, WebAuthn/passkey support, external secret-manager provisioning automation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Provider recovery-code MFA wired | Added a staged recovery-code MFA foundation for provider operators. Control Cloud provider operators now persist hashed one-time recovery codes in file and PostgreSQL stores, expose recovery-code reset under `/api/v1/provider-access/operators/{userId}/recovery-codes`, require and consume a recovery code during operator session creation once codes are configured, and block login after all codes are exhausted instead of silently downgrading to password-only. Control Desk now proxies recovery-code reset, carries optional recovery codes during operator sign-in, shows MFA/recovery-code metadata in the provider access panel, and displays generated codes only after reset. Verification passed: Control Cloud API build, Control Desk API build, provider-access smoke build/run with 12 checks, Control Desk provider-access proxy proof build/run with 18 checks, and Control Desk UI production build with the known large-chunk Vite warning. Remaining production work is TOTP/WebAuthn enrollment UX, external secret-manager provisioning automation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Provider access secret-file custody wired | Added file-backed custody for Control Cloud provider-access secrets. `ClientPortal:ProviderAccess` now supports `SharedSecretFile`, `SessionSigningSecretFile`, and per-key `SessionSigningKeys[*].SecretFile`, and Control Cloud hydrates those files at startup with fail-fast errors for missing or empty secret files. The runbook now documents the production file-backed secret shape for shared-secret bootstrap and signing-key rotation. Verification passed: Control Cloud API build, provider-access smoke build, and `ProviderAccessSmoke` passed 11 checks with the new file-sourced secret check. Remaining production work is provider-user MFA/recovery UX, external secret-manager provisioning automation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Provider access signing-key rotation hardened | Added a provider-access session signing key ring under `ClientPortal:ProviderAccess` with `ActiveSessionSigningKeyId` and `SessionSigningKeys`. Control Cloud still supports the legacy `SessionSigningSecret`, signs new bearer sessions with the active configured key, validates bearer sessions against configured keys plus the legacy secret for migration, and rejects old sessions once their previous signing key is removed. The provider access runbook now documents the rotation recipe and the smoke proof now covers acceptance during the rotation window plus rejection after key removal. Verification passed: Control Cloud API build with shared compilation disabled and `ProviderAccessSmoke` passed 10 checks. Remaining production work is provider-user MFA/recovery UX, external secret custody/provisioning, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Control Desk provider access proxy proof automated | Added `tools/SafarSuite.ControlDesk.ProviderAccessProxyProof`, a repeatable live proof that applies Control Cloud PostgreSQL migrations, starts Control Cloud and Control Desk on temporary local ports, points Control Desk at the Control Cloud provider access gate, and exercises the manager operations through only the Control Desk proxy routes. Verification passed: proof tool build and live proof passed 17 checks against Control Cloud `http://127.0.0.1:5161` and Control Desk `http://127.0.0.1:5190`, including list, create, scope update, suspend, admin password reset, reactivate, bearer session mint through the Control Desk operator-session proxy, under-scoped session override rejection ahead of the shared-secret fallback, manager-scoped session override list success, self-service password change through the Control Desk proxy, bearer session mint with the self-changed password, and final PostgreSQL state verification for operator `proxy.proof.40060bd8cb47479991dae5419d00da40@safarsuite.local` / user `13869bd651a642759715405b5b376abd`. Remaining production work is provider-user MFA/recovery UX, production secret custody/rotation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Control Desk provider access manager wired | Added a Control Desk provider access manager surface to the Client Desk Cloud workspace. Control Desk now proxies Control Cloud provider-operator list/create/password-reset/scope-update/status-update operations through layered application handlers and an HTTP provider-access client, and the Cloud tab can list operators, create a named operator with scoped rights, edit scopes, suspend/reactivate an operator, and reset a temporary password. Verification passed: Control Desk API build through alternate output and Control Desk UI production build with the known large-chunk Vite warning. Superseded by the provider self-change proof; remaining production work is provider-user MFA/recovery UX, production secret custody/rotation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-08 | Control Cloud PostgreSQL live proof automated | Added `tools/SafarSuite.ControlCloud.PostgresProof`, a repeatable proof that applies Control Cloud EF migrations, starts Control Cloud on a temporary local port with `Persistence:Provider=Postgres`, creates a provider operator through the real API, mints an operator bearer session from PostgreSQL, seeds a signed `EntitlementSnapshotIssued` envelope, creates a bootstrap package/setup token, registers a local-server installation, issues a signed entitlement bundle, reports heartbeat, queues/reads/acknowledges an installation command, reads installation status, and verifies the EF-backed rows across receipt, projection, provider operator, setup token, installation, entitlement issue, heartbeat, command, and acknowledgement tables. Verification passed: proof tool restore/build, local compose Postgres health, and live proof passed 17 checks against `http://127.0.0.1:5159` with client `1fc4a125-cb2e-45dc-9571-49182e16a018`, installation `postgres-proof-5205633e4f074fb9ae15fa1dc0ee9915`, entitlement version `639190496175459711`, bundle issue `718dc127-829c-4a77-9611-d53a5b8810dd`, heartbeat `c5459076-6078-42ea-81cc-bfd8826ad225`, and command `70952055-ae00-41ed-8843-66668a7cbd8f`. Superseded by the Control Desk provider access manager UI and self-change checkpoints for operator administration; remaining production work is MFA/recovery UX, production secret custody/rotation, and full clean-machine deployment proof with real deployment secrets. |
| 2026-07-07 | Provider access operational handoff captured | Added `docs/planning/control-cloud-provider-access-runbook-2026-07-07.md` so the provider-access work has an operator-facing handoff for configuration, PostgreSQL schema, first-operator bootstrap, scoped bearer sessions, operator administration, smoke verification, and stable failure-code triage. The runbook records the preferred operator-session path, the shared-secret compatibility path, scope catalog, one-time seed behavior, Control Desk bearer token config, and production guardrails around rotation and least-scope access. Verification passed: documentation diff check. Superseded by the Control Desk provider access manager UI and self-change checkpoints for operator administration; remaining production work is MFA/recovery UX, production secret custody/rotation, and clean-machine deployment proof with real deployment secrets. |
| 2026-07-07 | Durable provider operator admin wired | Added a durable provider-operator administration lane behind Control Cloud provider access. Provider operators are now stored in a file-backed `provider-access-operators.json` store seeded once from configuration, `provider-operators:manage` protects operator administration, and Control Cloud exposes list/create/password-reset/scope-update/status-update endpoints under `/api/v1/provider-access/operators`. Operator sessions now authenticate against the durable store, update `LastLoginAtUtc`, enforce assigned scopes, and write provider operator session/admin audit events. Verification passed: Control Cloud API build, compose proof runner build, and live admin proof on temporary Control Cloud `http://127.0.0.1:5143`: seeded admin listed operators with HTTP 200, created `ops.two@safarsuite.local` with HTTP 201, allowed its assigned `app-activation:read` login, denied over-scoped login with HTTP 403, reset password, rejected the old password with HTTP 401, suspended and rejected login with HTTP 401, reactivated, updated scopes, confirmed final list HTTP 200, persisted the operator store, and wrote 10 audit lines. Superseded by the PostgreSQL persistence, provider-access smoke proof, PostgreSQL live proof, Control Desk manager UI, and self-change slices; remaining production work is MFA/recovery UX, production secret custody/rotation, and clean customer setup UX. |
| 2026-07-07 | Provider operator session gate wired | Added the first provider-operator identity lane behind Control Cloud provider access. `POST /api/v1/provider-access/operator-sessions` validates configured provider operator credentials with the existing PBKDF2 password verifier, checks the requested scopes are assigned to that operator, and returns the same signed bearer provider session used by app activation and Client Portal management gates. Local development now includes a hashed `provider.admin@safarsuite.local` operator with app activation read/write and Client Portal manage scopes, and `ComposeBootstrapProof activate-app-runtime` defaults to the operator-session path before requesting app activation issuance. Verification passed: Control Cloud API build, compose proof runner build with shared compilation disabled after a Roslyn file lock, and live provider-operator proof on temporary Control Cloud `http://127.0.0.1:5141` where valid credentials returned a bearer token for actor `Local Provider Admin`, activation issue read returned HTTP 200, bad password returned `401 ProviderCredentialsInvalid`, and an unassigned scope request returned `403 ProviderAccessScopeDenied`. Superseded by durable provider-operator administration, PostgreSQL persistence, and the PostgreSQL live proof; remaining production work is MFA/password reset, audit around provider login/session issuance, production secret custody/rotation, and clean customer setup UX. |
| 2026-07-07 | Scoped provider access session gate wired | Added a production-shaped provider access session boundary in Control Cloud. `POST /api/v1/provider-access/sessions` mints signed bearer sessions from the transitional provider shared secret, sessions carry explicit scopes, app activation issue listing requires `app-activation:read`, app activation issue/revoke requires `app-activation:write`, and Client Portal invitation management requires `client-portal:manage`. The old `X-SafarSuite-Provider-Key` path remains as a compatibility fallback. Control Desk cloud clients can now send either `ProviderAccessToken` as bearer auth or the legacy provider secret, and `ComposeBootstrapProof activate-app-runtime` now uses the bearer-session path by default. Verification passed: Control Cloud API build, Control Desk API build, compose proof runner build, and live provider-session proof on temporary Control Cloud `http://127.0.0.1:5139` where read scope returned app activation issues with HTTP 200, read-only token write was denied with `403 ProviderAccessScopeDenied`, and write-scoped token reached normal installation validation. Superseded by provider-operator sessions, durable operator administration, and the PostgreSQL live proof; remaining production work is the real provider identity/login/MFA source, manager UX for issuing scoped sessions, production secret custody/rotation, and clean customer setup UX. |
| 2026-07-07 | App-runtime real-cloud activation proof passed | Ran the generated `app-runtime` Compose profile with the real SafarSuite app image and HTTPS-default Local API lane. Stub-cloud app-runtime boot proof passed at `https://127.0.0.1:18184` with app health `200` on `http://127.0.0.1:18185`. Then ran a real Control Cloud proof using file-persistence state under `artifacts\codex\real-cloud-app-runtime-activation-proof-1390155447`: Control Cloud on `http://127.0.0.1:5128`, container-facing cloud URL `http://host.docker.internal:5128`, client `77777777-7777-7777-7777-777777777777`, installation `real-cloud-app-runtime-main`, LocalServer port `18186`, app port `18187`, entitlement version `639190249093957034`, activation issue `844c451b-00eb-4ad3-8adf-1017af1ef75a`, and app server `6aecdcaa-1eec-47fd-9712-f7b8c8cb14e9`. Verification passed: generated real-cloud package, app-runtime Compose up, CA-pinned LocalServer verifier with bootstrap `Registered`, cloud registration `Active`, heartbeat `Received`/`Active`, `Accounting` module access `Active`/allowed, Control Cloud-issued app activation import, app activation status `Active`, app `Accounting` access `Active`/allowed, and Control Cloud activation issue register status `Issued`. The proof runner now emits explicit app health output, has fuller TLS diagnostics, and keeps a CA-pinned fallback chain check for Windows host verification. Superseded by the PostgreSQL live proof for database-backed cloud verification; remaining production work is real provider-user authorization beyond local proof keys, production secret custody/rotation, and clean customer setup UX. |
| 2026-07-07 | Compose runtime proof runner passed | Extended `tools\SafarSuite.LocalServer.ComposeBootstrapProof` with `run-compose` and `verify-running-runtime` modes. The runner now generates the HTTPS-default proof directory, starts the embedded stub cloud, runs Docker Compose, waits on the CA-pinned Local API host health endpoint, imports the signed bootstrap bundle, pulls entitlement, posts heartbeat, processes commands, checks runtime bootstrap/cache status, and verifies module-gateway access through the running LocalServer. Live proof passed with `https://127.0.0.1:18180`, `SAFARSUITE_LOCAL_API_TLS_MODE=GeneratedLocalCa`, bootstrap `Registered`, cloud registration `Active`, entitlement version `1`, heartbeat `Received`/`Active`, cached bootstrap/entitlement present, and `Accounting` module access `Active`/allowed. Also hardened the proof runner CA loader, aligned default container cloud URL with custom stub ports, made Dockerfile rebuilds robust when using a cached non-root app image as the runtime base, and confirmed compose cleanup leaves no containers running. Superseded by the app-runtime real-cloud activation proof and PostgreSQL live proof; remaining production work is secret custody/rotation, real provider-user authorization beyond local proof keys, and clean customer setup UX. |
| 2026-07-07 | HTTPS-default Local API deployment proof wired | Made generated LocalServer bootstrap artifacts default to `SAFARSUITE_LOCAL_API_TLS_MODE=GeneratedLocalCa`, `https://local-api:8080`, and `https://0.0.0.0:8080` while keeping `HttpOnly` as an explicit compatibility override. The installer now uses the generated host CA for local HTTPS health/import curl calls, Git Bash-safe OpenSSL subjects, durable PFX password reuse, and split server/trust mounts. `tools\SafarSuite.LocalServer.ComposeBootstrapProof` now writes proof CA/server PFX artifacts and HTTPS proof notes by default. Verification passed: Git Bash syntax check, HTTPS-default proof generation, installer default HTTPS proof, installer rerun password reuse proof, Control Cloud API build, Compose proof build, and `SafarSuite.LocalServer.EntitlementSmoke` build/run. Superseded by the PostgreSQL live proof for database-backed cloud verification; remaining hardening is production secret custody/rotation, real provider-user authorization beyond local proof keys, and full clean-machine app deployment proof with real deployment secrets. |
| 2026-07-07 | App shared Local API CA trust helper wired | Returned to the SafarSuite app workspace and moved the CA-pinned provider LocalServer HTTP client setup into reusable `LocalApiHttpClientFactory` infrastructure. App activation revocation still uses `SAFARSUITE_LOCAL_API_CA_CERTIFICATE_PATH`/`LocalServer:ControlLocalApiCaCertificatePath`, but the certificate-chain and hostname validation now live in one local API client gate for future outbound local API clients. Inspection confirmed the app backend hosts module-gateway GET/POST routes locally today, so there is no separate outbound module-gateway client to duplicate. Verification passed: app LocalServer build, ProductKernelGuardSmoke build, and `ProductKernelGuardSmoke` 42 checks with the HTTPS provider-stub revocation path. Superseded by the HTTPS-default Local API deployment proof for generated deployment defaults; remaining hardening is production secret custody/rotation and real provider-user authorization beyond local proof keys. |
| 2026-07-07 | Local API generated certificate automation wired | Added installer-side `SAFARSUITE_LOCAL_API_TLS_MODE=GeneratedLocalCa` automation that creates a local CA, server certificate, server PFX, trusted CA PEM, and durable private PFX password file during one-time setup. The generated Compose contract now splits mounts so the LocalServer API receives only `certs/local-api` server material and the SafarSuite app receives only `certs/trust` CA material. Bootstrap env/templates expose DNS/IP/day controls for certificate issuance. Verification passed: installer generated-cert proof, installer rerun password reuse proof, Git Bash syntax check, Control Cloud API build, `SafarSuite.LocalServer.EntitlementSmoke` build/run, and Compose proof tool build. Superseded by the shared app Local API CA trust and HTTPS-default Local API deployment checkpoints; remaining hardening is production secret custody/rotation and real provider-user authorization beyond local proof keys. |
| 2026-07-06 | HTTPS-ready LocalServer LAN transport contract wired | Kept the existing one-gate LocalServer API path working over HTTP by default, but made the API bind URL explicit through `SAFARSUITE_LOCAL_API_ASPNETCORE_URLS`, added optional Local API certificate path/password settings, mounted `./certs` into the LocalServer API and SafarSuite app runtime, and taught the app revocation client to trust a configured local CA certificate without disabling certificate validation. The provider bootstrap package, static installer, Compose proof generator, and entitlement smoke assertions now carry the same transport variables. Verification passed: provider LocalServer API build, `SafarSuite.LocalServer.EntitlementSmoke` build/run, Compose proof tool build, app LocalServer build, and app `ProductKernelGuardSmoke` 42 checks with HTTPS provider-stub revocation over a trusted local CA. Remaining production hardening is certificate issuance/trust automation, making HTTPS mandatory in the clean-machine deployment proof, production secret custody/rotation, and real provider-user authorization beyond local proof keys. |
| 2026-07-06 | Local API access key gate wired for app revocation checks | Added a scoped `LocalServer:RuntimeAccess`/`SAFARSUITE_LOCAL_API_ACCESS_KEY` gate to the provider LocalServer app-activation revocation-status endpoint. The route now requires `X-SafarSuite-Local-Api-Key` before it checks activation revocation state, the generated bootstrap env template carries the access-key placeholder, the install script auto-generates one when not supplied, and the SafarSuite app revocation client sends the header. Verification passed: provider LocalServer API build, `SafarSuite.LocalServer.EntitlementSmoke` build/run, app LocalServer build, and app `ProductKernelGuardSmoke` 42 checks with provider stub header assertion. Remaining hardening is authenticated HTTPS LAN transport, production secret custody/rotation, real provider-user authorization beyond local proof keys, and clean-machine deployment proof. |
| 2026-07-06 | SafarSuite app revocation enforcement hook wired | Returned to the SafarSuite app workspace and wired the app LocalServer to retain Control Cloud app activation issue metadata, call the provider LocalServer `POST /api/v1/local-server/app-activations/revocation-status` LAN authority, block login/writes when the imported app activation is revoked, and fail closed with `CheckUnavailable` when revocation status cannot be confirmed. The Windows client now preserves the activation issue/client/provider metadata during activation import and shows revocation state in the activation summary. Verification passed: app `ProductKernelGuardSmoke` 42 checks against a two-host provider/app setup and Windows client production build with the known large-chunk Vite warning. Remaining hardening is authenticated HTTPS LAN transport/provider auth beyond proof config, production key custody/rotation, and clean-machine app deployment proof. |
| 2026-07-06 | LocalServer app activation revocation status endpoint wired | Added shared `LocalServerAppActivationRevocationStatusRequest/Response` contracts, a LocalServer application handler, and `POST /api/v1/local-server/app-activations/revocation-status` so the SafarSuite app can ask the LAN authority whether an imported activation issue is revoked. The check requires the bootstrapped client/installation, compares app server id plus optional fingerprint/public-key hash, returns stable `Revoked`, `RevokedIdentityMismatch`, or `NotRevoked` states, and fails closed on identity mismatch. Verification passed: LocalServer API build through alternate output and `SafarSuite.LocalServer.EntitlementSmoke` build/run with revocation, mismatch, and not-revoked assertions. Superseded by the app revocation enforcement hook checkpoint; remaining hardening is real provider-user authorization beyond the local provider key, production key custody/rotation, authenticated HTTPS LAN transport, and clean-machine deployment proof. |
| 2026-07-06 | App activation revocation command sync wired | Cloud-side app activation revoke now signs and queues a `revoke_app_activation` installation command with the revoked issue, app server, fingerprint, key hash, actor, reason, and revocation time. LocalServer verifies that command through the existing signed command lane, persists it in a durable app-activation revocation ledger, and acknowledges it. Verification passed: Control Cloud API build through alternate output, LocalServer API build through alternate output, and `SafarSuite.LocalServer.EntitlementSmoke` build/run with three applied commands and three acknowledgements. Superseded by the app revocation enforcement and local API access-key checkpoints; remaining hardening is authenticated HTTPS LAN transport, real provider-user authorization beyond the local provider key, and production secret custody/rotation. |
| 2026-07-06 | App activation replacement rotation link wired | Added explicit replacement lineage for SafarSuite app activation issues. Issuance requests can now carry `replacesActivationIssueId`; Control Cloud verifies the replaced issue exists, belongs to the same client/provider installation, and is already revoked before issuing a new activation token. The issue store, list/revoke responses, Control Desk proxy, and Client Desk Cloud tab now show the replacement id, and revoked rows can prepare a replacement while requiring a fresh app activation request/public key import before re-issue. Verification passed: Control Cloud API build through alternate output, Control Desk API build through alternate output, and Control Desk UI production build with the known large-chunk Vite warning. Superseded by the revocation-command and app enforcement checkpoints; remaining hardening is real provider-user authorization beyond the local provider key, production key custody/rotation, authenticated HTTPS LAN transport, and clean-machine deployment proof. |
| 2026-07-06 | App activation mapping revoke flow wired | Added Cloud-owned revoke behavior for app activation issues. Control Cloud now supports provider-gated `POST /api/v1/control-cloud/clients/{clientId}/app-activation-issues/{activationIssueId}/revoke`, marks the issue revoked with actor/reason/time, and records `AppActivationTokenRevoked` audit. Control Desk proxies the revoke command, and the Client Desk Cloud tab now has revoked-by/reason fields plus a per-issue revoke action in the activation register. Verification passed: Control Cloud API build through alternate output, Control Desk API build through alternate output, and Control Desk UI production build with the known large-chunk Vite warning. Superseded by the replacement-link, revocation-command, and app enforcement checkpoints; remaining hardening is real provider-user authorization beyond the local provider key, production key custody/rotation, authenticated HTTPS LAN transport, and clean-machine deployment proof. |
| 2026-07-06 | Durable app activation mapping register wired | Added a Cloud-owned app activation issue record and file-backed register for provider-installation -> app-server mappings. Control Cloud now records structured activation issue metadata during token issuance, exposes provider-gated `GET /api/v1/control-cloud/clients/{clientId}/app-activation-issues` with installation/app-server/query/take filters, Control Desk proxies the register, and the Client Desk Cloud tab shows a searchable activation register beside import/issue/download. Verification passed: Control Cloud API build through alternate output, Control Desk API build through alternate output, and Control Desk UI production build with the known large-chunk Vite warning. Superseded by the revoke, replacement-link, revocation-command, and app enforcement checkpoints for register control; remaining hardening is real provider-user authorization beyond the local provider key, production key custody/rotation, authenticated HTTPS LAN transport, and clean-machine deployment proof. |
| 2026-07-06 | Control Desk app/provider identity mapping UX wired | Strengthened the Client Desk Cloud tab so provider staff can import `safarsuite-app-activation-request-v1` JSON from the SafarSuite app, auto-fill app server id/fingerprint/public key/requested-by values, see the provider LocalServer installation id mapped against the app LocalServer identity before issuing, and see the issued provider-installation -> app-server mapping in the Cloud control register and activation import result. Verification passed: Control Desk UI production build with the known large-chunk Vite warning. Superseded by the durable register and revoke checkpoints; remaining hardening is real provider-user authorization beyond the local provider key and production key custody/rotation. |
| 2026-07-06 | SafarSuite app setup-token authority UX wired | Returned to the app workspace and polished the first-manager signed-token path. Device Manager can now import first-manager setup-token JSON instead of requiring hand-paste of token/signature/key id, decodes the token claims locally for diagnostics, shows whether the token is bound to the current LocalServer installation id and pending device id, and adds LocalServer id/fingerprint to the activation summary. Verification passed: Windows client production build with the known large-chunk Vite warning, app workspace `git diff --check`, and provider docs `git diff --check`. Remaining app/provider hardening is production provider-user authorization, key custody/rotation, and clearer provider-installation to app-server identity mapping in Control Desk/Cloud. |
| 2026-07-05 | SafarSuite app pre-login manager setup wired | Returned to the app workspace and connected the Windows pre-login activation panel to the shared Device Manager screen behind `Manager Setup`. The one-time customer setup path can now continue from activation import into first-device bootstrap, first-admin creation, manager login, and device approval before normal sign-in, and a pending pairing result from sign-in opens that same setup panel. A successful first-admin or manager session transitions directly into the authenticated workspace. Verification passed: Windows client production build with the known large-chunk Vite warning, app workspace `git diff --check`, and provider docs `git diff --check`. Remaining app-side polish is setup-token authority UX, clearer provider/app installation identity mapping diagnostics, and production key/provider-user hardening. |
| 2026-07-05 | SafarSuite app pre-login activation UX wired | Switched to the app workspace and added the first-manager-facing activation bridge on the Windows pre-login surface: it shows LocalServer activation status, server installation id, and fingerprint; downloads `safarsuite-app-activation-request-v1` JSON for Control Desk; imports the signed Control Desk activation JSON through `POST /api/local-server/activation-token`; accepts either raw activation import JSON or an object with `import`; and refreshes product state after import. Verification passed: Windows client production build with the known large-chunk Vite warning and app workspace `git diff --check`. The prior running Control Desk and Control Cloud API processes were also stopped before the app-side work. |
| 2026-07-05 | Control Desk app activation manager flow wired | Protected the Control Cloud app activation issuer with the provider access gate, added the Control Desk application/API proxy for `POST /api/v1/control-cloud/clients/{clientId}/installations/{installationId}/app-activation-token`, and added Cloud tab controls for provider staff to enter the app server identity, issue the activation import, see activation status in the cloud control register, and download the signed SafarSuite app activation JSON. The compose proof tool now sends the provider access header when exercising `activate-app-runtime`. Verification passed: Control Desk API build through alternate output, Control Cloud API build through alternate output, compose bootstrap proof build through alternate output, and Control Desk UI production build with the known large-chunk Vite warning. Remaining production work is real provider-user authorization beyond the local provider key, key custody/rotation, and app-side first-manager UX/identity mapping diagnostics. |
| 2026-07-05 | Control Cloud-owned app activation issuance proof passed | Added the first production-shaped app activation issuer in Control Cloud: `GET /api/v1/control-cloud/app-activation/signing-key` exposes the active verifier key, `POST /api/v1/control-cloud/clients/{clientId}/installations/{installationId}/app-activation-token` validates the registered installation, derives app module grants from the latest signed entitlement bundle issue, signs the app activation token with the Control Cloud app-activation ECDSA key, returns the app import payload, and records `AppActivationTokenIssued` audit. `tools\SafarSuite.LocalServer.ComposeBootstrapProof activate-app-runtime` now calls Control Cloud for token issuance instead of signing locally. Live proof passed with package `b9a8d195-d9e5-4580-b7a3-6a4fa8acb4f2`, client `66666666-6666-6666-6666-666666666666`, installation `app-cloud-issuer-main`, LocalServer port `18086`, app port `18087`, entitlement version `639188628034465418`, activation issue `c250ff3f-a054-4fb2-ba39-4f076436d87a`, app server `9c7a6ff9-0b00-4fe8-a23e-01e8489a701d`, app `Accounting` access `Active`/allowed, and Control Cloud audit event `c4dd74c6-8115-44dd-9397-16ce2b13211d`. Remaining production work is manager first-setup UX, key rotation/secret storage beyond local proof config, and identity mapping visibility between app server id and provider LocalServer installation id. |
| 2026-07-05 | App activation bridge proof passed | Extended `tools\SafarSuite.LocalServer.ComposeBootstrapProof` with `activate-app-runtime` mode and replaced the app proof activation verifier with a non-development P-256 key pair (`compose-proof-ecdsa-p256-2026-07`) so the SafarSuite app passes its production readiness guard. The generated real Control Cloud package `cc9d55e9-97ee-4d56-a8d7-637f9832970f` started LocalServer plus `safarsuite-app` through `--profile app-runtime` on LocalServer port `18084` and app port `18085`; LocalServer imported bootstrap, pulled entitlement version `639188570961473290`, posted heartbeat as `Active`, and allowed `Accounting`. The new activation bridge read the app activation state, issued an app activation token bound to the app's internal server installation id `cc3b011e-9af4-4bed-bf7c-11bacb3e4356`, imported it, and verified the app's own `Accounting` module endpoint returned `Active`/allowed. This proved the deployment could coordinate LocalServer, Control Cloud, and the app runtime without manual re-setup in the proof harness, and is superseded by the durable activation register, revoke/replacement, app enforcement, and local API access-key checkpoints. |
| 2026-07-05 | App-runtime profile boot proof passed with activation gap | Ran the real Control Cloud Compose proof with `--profile app-runtime` enabled. The first run correctly failed the SafarSuite app production readiness guard because deployment secrets/signing/session configuration were missing. Added proof-only app secret/public-key values to `tools\SafarSuite.LocalServer.ComposeBootstrapProof` generated env output, reran the app profile, and verified `safarsuite-app` started healthy on host port `18083` beside `local-db`, `local-api`, `local-worker`, and `local-agent`. LocalServer still registered through real Control Cloud, pulled entitlement version `639188534498708273`, posted heartbeat with license state `Active`, and allowed `Accounting` through the module gateway. The app container received `SAFARSUITE_MODULE_GATEWAY_URL=http://local-api:8080`, `SAFARSUITE_LOCAL_API_BASE_URL=http://local-api:8080`, `SAFARSUITE_INSTALLATION_ID=app-profile-main`, and the client id, but the app image's own module endpoint returned `StatusInactive`/`NotActivated` with its internal installation id, so the remaining app-side work is first-manager/activation bootstrap plus making the app runtime consume the external LocalServer authority rather than staying internally unactivated. |
| 2026-07-05 | Real Control Cloud LocalServer Compose proof passed | Extended `tools\SafarSuite.LocalServer.ComposeBootstrapProof` with `generate-real-cloud` mode. The tool now seeds a disposable real Control Cloud through the signed `EntitlementSnapshotIssued` envelope receiver, asks the real Control Cloud bootstrap endpoint for a package, writes the returned Compose/env/runtime manifest/signed bundle artifacts, and keeps local image/port overrides for repeatable proof runs. Verification passed with a file-persistence Control Cloud API on `http://127.0.0.1:5127` and container-facing `http://host.docker.internal:5127`: generated seed status `Accepted`, created bootstrap package `7d477ee1-8289-40b7-bb3e-3676db7923b0`, started `local-db`, `local-api`, `local-worker`, and `local-agent`, imported the signed bundle with registration status `Registered`, pulled entitlement version `639188214853173924`, posted heartbeat with license state `Active`, confirmed `Accounting` module gateway access was allowed, read Control Cloud status as installation `Active` with latest entitlement/heartbeat stored, and tore the stack/API down cleanly. The remaining deployment proof is the optional `app-runtime` profile and production registry/base image path. |
| 2026-07-05 | Install-shaped LocalServer Compose proof passed | Added `tools\SafarSuite.LocalServer.ComposeBootstrapProof` to generate a customer-install-shaped Compose directory and run a tiny Control Cloud stub. The proof started `local-db`, `local-api`, `local-worker`, and `local-agent` from the generated package using local cached images, verified local API `/health` returned 200, imported the signed bootstrap bundle through the local API with bootstrap registration status `Registered`, pulled entitlement version 1, posted heartbeat with license state `Active`, confirmed `Accounting` module gateway access was allowed, and tore the stack/stub down cleanly. This proves the LocalServer-only one-time bootstrap runtime loop; the real Control Cloud seeded install, optional `app-runtime` profile, production registry image, and MCR-backed default base path remain pending. |
| 2026-07-05 | LocalServer runtime image wrapper started | Added the first provider-office LocalServer Docker wrapper with `safarsuite-local-api`, `safarsuite-local-worker`, and `safarsuite-local-agent` command shims. The shims map installer `SAFARSUITE_*` values into `LocalServer:*` runtime config, share `/var/lib/safarsuite/local-server` state, and split automation so worker handles entitlement pull/heartbeat while agent handles command polling. Bootstrap Compose templates now mount `runtime-services.manifest.json` into LocalServer and app containers, the env template exposes config/state dirs, and `install.sh` can import a supplied signed bootstrap bundle through the local API after Compose starts. Verification passed: LocalServer API build, LocalServer API Release publish, base and `app-runtime` Docker Compose config parsing from a generated install-shaped directory, built `safarsuite-local-server:codex-smoke` from the `runtime-from-publish` target using the cached ASP.NET-derived app image as a local-only base override, container `/health` returned 200, and `tools\SafarSuite.LocalServer.EntitlementSmoke` passed. Production image build/push still needs MCR access because the default .NET SDK/runtime base image metadata requests continue to return EOF and neither official base image is cached locally. |
| 2026-07-05 | Bootstrap deployment contract smoke hardened | Strengthened `tools\SafarSuite.LocalServer.EntitlementSmoke` so the bootstrap proof now checks the generated Docker Compose artifact, environment template, runtime services manifest, signed bundle checksum, payload checksum, optional `safarsuite-app` app-runtime profile, app image variable, app host port variable, module-gateway URL, and site/deployment identity placeholders. Verification passed with `dotnet run --no-restore --project tools\SafarSuite.LocalServer.EntitlementSmoke\SafarSuite.LocalServer.EntitlementSmoke.csproj`. |
| 2026-07-05 | Milestone stabilization path started | Added `docs/planning/milestone-stabilization-runbook-2026-07-05.md` and completed the first stabilization pass. App verification passed: LocalServer build, ProductKernelGuardSmoke 40 checks, ReadOnlyEnforcementSmoke 24 checks, ReportExecutionSmoke 30 checks, runtime Compose config, and `git diff --check`. Control Desk verification passed: API build through alternate output, UI production build with the known Vite chunk warning, ProductAccessCatalogSmoke 4 checks, and `git diff --check`. The tracked app `.vs` cache churn was restored and app `.vs` status is clean. The three-service live golden path passed with signed catalog command `44ff965f-b934-4ba1-8677-0f0b59a7cb1f`, app readback of 7 groups and 6 resources, `Reports` gateway `Active`/allowed, and runtime profile v1. The Control Desk operator-register UI work is split into `docs/planning/control-desk-operator-register-ui-review-2026-07-05.md`; its UI build and targeted whitespace check passed. |
| 2026-07-05 | App runtime health/profile/log diagnostics landed | In `C:\Users\Daniyal\Documents\Codex\2026-06-09\hello-there-2`, the app LocalServer now exposes `safarsuite-local-runtime-profile-v1` through `GET /api/v1/local-server/runtime/profile` and embeds the same runtime profile in `GET /health`. The profile is read from the Control bootstrap/runtime env contract, including app version, deployment mode, site id, site role, optional parent site, branch code, sync topology, local API URL, module gateway URL, and runtime manifest path. Module-gateway decisions now write structured logs with module, allowed state, access state, deployment mode, site id, site role, requested installation id, and local installation id, giving Control-side log-tail diagnostics app-runtime context. App verification passed: LocalServer build, ProductKernelGuardSmoke 40 checks, ReadOnlyEnforcementSmoke 24 checks, ReportExecutionSmoke 30 checks, runtime Compose config, and `git diff --check`. |
| 2026-07-05 | Product access catalog persistence smoke automated | Added `tools\SafarSuite.ControlDesk.ProductAccessCatalogSmoke`. The smoke reads the live Control Desk catalog, saves a temporary owner-managed Payroll group/resource, verifies persisted readback and resolver output, restores the original catalog by default, and can optionally publish/import a signed app product-kernel catalog command when supplied an activation request id and LocalServer URL. Build passed and the default live smoke passed 4 checks against `http://localhost:5188`. |
| 2026-07-04 | Local-server app-runtime diagnostics strengthened | Command-triggered local-server diagnostics now include signed runtime-manifest intent in each service detail: service role, Compose profile, image environment variable, host port environment variable, internal URL, health URL, and dependencies. The optional `safarsuite-app` slot is still correctly reported as `ProfileDisabled` when the app profile is not started, but the diagnostic bundle now proves it is wired to `SAFARSUITE_APP_IMAGE`, `SAFARSUITE_APP_HTTP_PORT`, and `http://safarsuite-app:5280/health`. `tools\SafarSuite.LocalServer.EntitlementSmoke`, the LocalServer infrastructure build, the entitlement-smoke build, and `git diff --check` passed. |
| 2026-07-04 | Control Desk product access catalog owner editing persisted | Added owner-managed product access catalog persistence for Control Desk. `PUT /api/v1/contracts/product-access-catalog` now saves module groups/resources, reads and publish use the saved catalog when present, PostgreSQL stores the catalog in `control.product_access_catalogs` with an in-memory dev/test repository, and the Contracts `Access catalog` view can add/update/remove groups and resources before publishing to the app product kernel. |
| 2026-07-04 | Accounting/GL MVP checkpoint captured | See `docs/planning/accounting-gl-mvp-checkpoint-2026-07-04.md`. Current GL slice is directionally aligned with legacy controlled COA/voucher/report evidence and is verified through build, UI build, accounting smoke, live default-control probe, and diff whitespace check. Baseline includes MAIN-only controlled COA ranges, hierarchy guards, reconciliation/repair-plan review, default GL controls, periods, manual journals, trial balance, profit and loss, balance sheet, period close handoff, and close-journal drill-in. Remaining gaps are opening balances, voucher type/numbering setup, branch/adjusted-date reporting, legacy import dry-run, guided mutating repairs, and field-by-field legacy parity. |
| 2026-07-04 | Control Desk product access catalog publish proved against real app workspace | Live cross-workspace smoke used existing Control Desk API on `http://127.0.0.1:5188`, app CloudServer on `http://127.0.0.1:5281`, and app LocalServer on `http://127.0.0.1:5290`. Control Desk `POST /api/v1/contracts/product-access-catalog/product-kernel-command` requested an app-cloud-signed `SetProductAccessCatalog` command, the app LocalServer imported the activation token and applied the returned command through `/api/product-kernel/vendor-commands`, and `/api/product-kernel/state` readback showed 7 catalog groups and 6 resources including `reporting` and `reports.execute`. App `tests\ProductKernelGuardSmoke` now passed 37 checks, including app CloudServer-issued catalog command import. |
| 2026-07-04 | Real app module-gateway enforcement and runtime wrapper verified | In `C:\Users\Daniyal\Documents\Codex\2026-06-09\hello-there-2`, the app local-server exposes the v1 module-gateway routes, the Windows client folds gateway `isAllowed` into menu/window access, backend `LocalWritePolicy` denies writes when the gateway denies a module, and reporting execution/audit now require `reporting-core` gateway access while still allowing read-only `Restricted` report access. `Dockerfile.localserver`, `.dockerignore`, `docker-compose.runtime.yml`, and `docker/localserver.env.template` now define the first app runtime image/Compose contract. Control Cloud bootstrap templates now point the optional `safarsuite-app` slot at `ghcr.io/danionwheels/localserver`, port `5280`, and `http://safarsuite-app:5280/health`. `tests\ProductKernelGuardSmoke` passed 30 checks, `tests\ReadOnlyEnforcementSmoke` passed 24 checks, `tests\ReportExecutionSmoke` passed 28 checks, Windows client production build passed, local-server Release publish passed, Compose config validation passed, the published LocalServer internal healthcheck passed, Docker image build passed, Compose runtime started healthy on app port `5280` and Postgres host port `55632`, 39 local migrations applied, v1 module-gateway container probes returned the expected contract shape, pushed GHCR image `ghcr.io/danionwheels/localserver:0.1.0` with digest `sha256:0deacfd234d59354d7560371d9b475633903e7d18dd0a84cb5cfbb0cdb182ba1`, anonymous manifest/pull checks passed using an empty Docker config, and the Control Desk local-server entitlement/bootstrap smoke passed. |
| 2026-07-03 | Control spine live proof passed; app runtime probe ready | See `docs/planning/control-spine-checkpoint-2026-07-03.md`, `docs/planning/control-spine-demo-runbook.md`, and `docs/planning/safarsuite-app-integration-handoff.md`; solution build, UI build, accounting smoke, local-server entitlement/command-processing smoke, app runtime probe self-test, live PostgreSQL publishing, bootstrap registration, entitlement pull, heartbeat, diagnostics upload, command acknowledgement, and module-gateway access passed. Command-triggered local-server diagnostics now collect the signed runtime manifest, Docker/Compose availability, live Compose service state, recent warning/error log-tail lines, and the optional `safarsuite-app` profile slot. `tools/SafarSuite.AppRuntimeProbe` is now the placeholder app-side consumer of the module-gateway v1 contract. Next recommended move is the real SafarSuite app workspace module-gateway integration. |

## Scope Rule

SafarSuite Control Desk is a client maintenance system first. Accounting, billing, payments, and cloud publishing stay active only when they support maintaining a client, charging that client, or controlling that client's access. Survey/FAS work is not part of the active product surface.

## Active Work

| Work item | Module | Status | Notes |
| --- | --- | --- | --- |
| Layered architecture contract | Architecture | Done | See `docs/architecture/layered-architecture.md` |
| Canonical product direction | Architecture/Product | Done | See `docs/architecture/product-direction.md`; records the north-star goal, system responsibilities, module-control chain, communication boundaries, non-goals, and drift-check questions for Control Desk, Control Cloud, Client Portal, and SafarSuite |
| Canonical product naming | Architecture | Done | See `docs/architecture/product-naming.md` |
| Client runtime communication and deployment blueprint | Architecture/Product/ControlCloud/LocalServer/SafarSuiteApp | Done | See `docs/architecture/client-runtime-communication-and-deployment-blueprint.md`; records the one-gate LAN/runtime model, one-time setup flow, Control Cloud outbound control path, manager rights boundary, module-gateway rules, Docker Compose bootstrap decision, reliability/security rules, and the separation from future operational business-data sync |
| Cloud/local communication map | Architecture/ControlCloud/LocalServer | Done | See `docs/architecture/cloud-local-communication-map.md`; records Control Desk, Control Cloud, Client Portal, and SafarSuite local-server communication paths for license, heartbeat, command, and portal alignment |
| Client deployment modes and data-sync boundary | Architecture/Product/ControlCloud/LocalServer | Done | See `docs/architecture/client-deployment-and-data-sync-boundary.md`; SafarSuite supports offline local, branch-to-HQ sync, cloud-sync multi-branch, and hosted SaaS modes while Control Cloud remains the commercial/control channel and operational branch/HQ/cloud business-data sync stays in a separate future data plane |
| Product module catalog boundary | Contracts/Entitlements/Billing/Frontend/Architecture | Basic done | See `docs/architecture/product-module-catalog-boundary.md`; records the flexible IncludedForAll/PaidAddOn module catalog, seeds temporary dummy modules with billing defaults, enforces active-catalog module selection during contract create/replace, lets the contract UI use catalog checkboxes, lets billing setup prefill charge-code/rule forms from active paid add-ons, and persists the originating module code on saved charge rules and generated invoice draft lines |
| Legacy source reference | Legacy | Done | See `docs/legacy/source-reference.md` |
| Travel/Champion accounting sweep | Legacy/Accounting/Travel | Accepted | See `docs/planning/travel-legacy-sweep.md` and `docs/planning/accounting-gl-foundation-sweep.md`; records `TRV.mdb`, `TRV.mde`, and `Data.sql` evidence for Control Desk accounting direction: controlled COA, party ledger identities, voucher/journal posting, invoice/receipt/refund chains, tax/discount setup, branch/currency context, and later Travel/Tour module parity |
| Backend solution scaffold | Platform | Done | Created solution and projects for Api, Application, Domain, Infrastructure, Contracts |
| Frontend scaffold | Platform | Done | Vite React TypeScript app created under `apps/control-desk-ui` |
| Client maintenance UI | Clients/Frontend | Done | Active frontend now opens the client desk with list, create, detail, edit, lifecycle, support notes, and accounting-profile status |
| First domain model draft | Clients/Contracts/Billing/Payments/Entitlements/Audit | Done | Core aggregate roots and repository ports created |
| Client maintenance basics | Clients | Done | Client detail, edit, activate, and suspend API actions are wired |
| Client contacts | Clients | Done | Add/list structured contacts with roles and primary flag; active UI shows contacts |
| Client support notes | Clients | Done | Add/list internal client notes; client detail includes note history |
| PostgreSQL local database foundation | Persistence | Done | Docker Compose, local EF tool, EF DbContext, and initial migration are in place |
| Client maintenance persistence | Clients/Persistence | Done | PostgreSQL slice persists clients, contacts, and support notes |
| Client contract persistence | Contracts/Persistence | Done | PostgreSQL slice persists active client contracts and module allowances |
| Client contract maintenance | Contracts/Frontend | Basic done | Client desk can list contracts, create a contract, replace the active contract, and suspend active contracts |
| Accounting persistence | Accounting/Persistence | Done | PostgreSQL slice persists ledger accounts, journal entries, and journal lines |
| Billing/Profile/Outbox persistence | Billing/Clients/ControlCloud/Persistence | Done | PostgreSQL slice persists client accounting profiles, charge codes, client charge rules, invoices, invoice lines, and cloud outbox messages |
| Payment persistence | Payments/Persistence | Done | PostgreSQL slice persists invoice payments, including pending-review bank transfers, approved receipts, invoice balance/status updates, and receipt journals |
| Client billing setup UI | Billing/Frontend | Basic done | Client desk can maintain accounting profile, charge/tax setup, invoice draft, and invoice issue flow; ledger account setup now shows enforced GL defaults, uses `GL_Working.xlsx`-aligned controlled numeric account-code suggestions, and auto-links the AR account to the client profile |
| Client payment and entitlement UI | Payments/Entitlements/Frontend | Basic done | Client desk can record invoice payments, review bank transfers, reverse approved payments, apply/refund client credit balances, and issue/refresh local entitlement snapshots |
| Client statement/receivables UI | Clients/Billing/Accounting/Frontend | Basic done | Client desk can show invoices, payments, credit notes, applied credit, client refunds, available credit, running receivable balance, and related journal postings |
| Accounting/GL correction sweep | Accounting/Billing | Done | Invoice creation split clarified in `accounting-gl-foundation-sweep.md` |
| Controlled COA setup backlog | Accounting/Billing/Frontend | GL MVP checkpoint done | `docs/planning/accounting-gl-foundation-sweep.md` tracks the `GL_Working.xlsx`-aligned backlog and `docs/planning/accounting-gl-mvp-checkpoint-2026-07-04.md` freezes the current baseline. Accounting Setup persists company-scoped ranges, role mappings drive suggestions, ledger account creation is range/type/balance/posting/level guarded, Header/Total ranges exist for major classes, Subsidiary creation requires a Control parent, the Accounting workspace can maintain the COA/register/ranges, and reconciliation plus repair-plan review remain read-only. Deferred COA polish must preserve the legacy open-ended nested register where `Total` rows are visible tree rows and users can create child/grandchild/deeper accounts inline under selected context. Remaining work is legacy import dry-run, opening balances, and deliberate guided repair actions. |
| Accounting/Billing foundation | Accounting/Billing | In Progress | Dynamic charge/tax setup, invoice draft generation, profile-assisted invoice issue GL posting, unpaid invoice voiding, full credit notes, client refunds, credit settlement, payment review/reversal, signed local outbox publishing, journal/ledger read models, client statement visibility, full-chain accounting smoke coverage, UI guardrails, GL reports, period guards, default GL controls, period close handoff, close-journal drill-in, the first Control Cloud receiver boundary, PostgreSQL-backed cloud commercial projection, installation-bound cloud entitlement bundle issue audit, signed installation command acknowledgements, local-server setup-token registration, local-server entitlement pull/verification/cache/feature gates, paid-module guardrails, heartbeat/status, Control Desk cloud status panel, portal preview, portal invite/session audit, offline renewal file fallback, local clock/replay trust state, local import audit persistence, bootstrap generation/templates, runtime service manifest boundary, setup/bootstrap/registration audit visibility, diagnostics export/upload, runnable local-server API host, product catalog boundary, real app workspace module-gateway enforcement, app runtime health/profile/log diagnostics, real Control Cloud LocalServer Compose proof, app-runtime container boot proof, proof-harness app activation bridge, Control Desk-managed app activation issuance, durable app activation mapping register, revoke, replacement linkage, signed revocation command sync to LocalServer, and Control Desk provider access operator management are wired; portal payment UI, SafarSuite app runtime deactivation from LocalServer revocations, app-side first-manager production UX, production secret provisioning/key rotation, catalog management screens, opening balances, voucher numbering/type setup, branch-aware reporting, and legacy report parity are still pending |
| Client accounting profile | Clients/Accounting | Done | Client can be linked to AR/default currency/cloud identity; invoice issue can resolve AR from the profile, and the client desk no longer requires manually pasting the AR ledger account id during normal setup |
| Cloud invoice outbox | Billing/ControlCloud | Done for local loop | Invoice issue creates persisted `InvoiceIssued` messages, and the local publisher can mark signed pending outbox messages sent |
| Payment outbox events | Payments/ControlCloud | Done | Approved receipt posting enqueues persisted `PaymentRecorded` and `ClientPaidStatusChanged`; reversals enqueue `PaymentReversed` and reopen paid-status when needed |
| Local outbox publisher | ControlCloud | Done | Manual dev endpoint builds signed envelopes and marks ready outbox messages sent/failed without calling SafarSuite Control Cloud |
| Control Cloud publish readiness | ControlCloud | Basic done | Signed payload envelope, local/HTTP publisher adapter, retry attempt metadata, and config-driven publish mode are wired |
| Control Desk to Control Cloud local publish | ControlCloud/Integration | Basic done | Control Desk Development config points HTTP publishing to the local Control Cloud receiver, and the accounting smoke can publish seven real outbox rows through the receiver |
| Client entitlement chain | Entitlements/ControlCloud/LocalServer | In Progress | Paid invoices can now issue persisted local entitlement snapshots from active contract defaults and enqueue signed `EntitlementSnapshotIssued` publish messages; cloud-side commercial projection accepts entitlement snapshots, registers installations through one-time setup tokens, issues installation-bound audited HMAC-signed portal/local-server entitlement bundles, exports offline renewal files, queues signed installation commands with acknowledgement audit, and local-server libraries can register, pull, import, cache, gate, reject replay, record local import audit, record clock warnings, and report heartbeat/license state |
| Offline entitlement control rules | Entitlements/ControlCloud/Client Portal | Accepted | `docs/planning/offline-entitlement-control-rules.md` defines paid-period offline validity, heartbeat separation, warning/grace/restriction states, offline renewal files, trust-based lease lengths, command acknowledgements, replay protection, and audit requirements |
| Control Cloud deployment tracker | ControlCloud/Client Portal/Local Server | Accepted | `docs/planning/control-cloud-deployment-tracker.md` defines the one-cloud rule, portal-controlled/local-server-pulled deployment, V1 Docker Compose bootstrap bundle, and later `.deb` packaging path |
| Control Cloud receiver skeleton | ControlCloud/API/Application/Infrastructure | Basic done | `SafarSuite.ControlCloud.Api` receives signed Control Desk envelopes, validates payload hash/HMAC, records accepted/rejected/duplicate receipts in PostgreSQL, projects accepted commercial messages into a portal-readable read model, and returns stable cloud message references |
| Control Cloud PostgreSQL persistence | ControlCloud/Persistence | Done | Cloud receiver receipts and client commercial projections are persisted in PostgreSQL under the `cloud` schema with a filtered unique accepted-idempotency index |
| Control Cloud portal commercial projection | ControlCloud/ClientPortal | Basic done | Accepted invoice, payment, credit note, refund, credit-application, paid-status, and entitlement-snapshot envelopes update PostgreSQL-backed `GET /api/v1/client-portal/clients/{clientId}/commercial-summary` |
| Client Portal identity/session boundary | ControlDesk/ControlCloud/ClientPortal | Basic invite management foundation done | `docs/architecture/client-portal-identity-boundary.md` records that Control Cloud owns portal credentials; Control Desk can request, list, resend, and revoke portal invites for client contacts, Control Cloud protects invitation management with a provider key, invitation delivery is pluggable between local file and SMTP, invite/session audit records are written, and invite acceptance creates password-backed client-scoped sessions; real provider users, MFA, password reset, and production mail retry handling remain pending |
| Control Cloud entitlement signing boundary | ControlCloud/ClientPortal/LocalServer/Entitlements | Basic done | Latest projected entitlement snapshots can be returned as installation-bound HMAC-signed bundles through the session-protected Client Portal route, the machine-facing `GET /api/v1/local-server/installations/{installationId}/entitlement-bundle?clientId={clientId}` route, and the offline renewal file export route; bundle issues and installation registry state are persisted in PostgreSQL |
| Control Cloud installation command queue | ControlCloud/LocalServer/Entitlements | Basic done | Control Cloud can queue signed monotonic commands for registered installations, local servers can pull pending commands and acknowledge Applied/Failed/Rejected results, PostgreSQL persists command plus acknowledgement audit rows, and command signatures are stable across `jsonb` payload reordering plus microsecond timestamp precision round-trips |
| Control Cloud installation heartbeat | ControlCloud/LocalServer | Basic done | Local servers can report heartbeat to `POST /api/v1/local-server/installations/{installationId}/heartbeat`; Control Cloud stores heartbeat status separately from reported license state in PostgreSQL/file persistence |
| Control Cloud installation status view | ControlCloud/ClientPortal/ControlDesk | Pairing status visible in portal preview | Shared status is available through Control Cloud and Client Portal routes; the Control Desk client page can refresh by installation id, and `/client-portal/index.html` can show commercial, license, heartbeat, entitlement, command, and pairing status for a client/installation |
| Control Cloud setup-token registration | ControlCloud/LocalServer | Basic done | Control Cloud can create one-time hashed setup tokens for client installations, local servers can register outward with token scope/expiry checks, and entitlement issue now requires a registered installation |
| Control Cloud bootstrap package generation | ControlCloud/LocalServer | Real Control Cloud proof passed | Control Cloud can generate a setup-token-backed bootstrap package with cloud registration URLs, entitlement/heartbeat/command/diagnostics endpoints, a copyable install command, signed JSON bootstrap bundle download, artifact checksums, served `install.sh`, Docker Compose template, environment template, runtime services manifest, and optional SafarSuite app profile slot; the installer can now import a supplied signed bootstrap bundle through the local API after Compose starts, the LocalServer image wrapper provides the `safarsuite-local-api`, `safarsuite-local-worker`, and `safarsuite-local-agent` commands expected by Compose, and `tools\SafarSuite.LocalServer.ComposeBootstrapProof` has proved the generated LocalServer-only runtime loop first against a stub cloud and now against a disposable real Control Cloud API |
| Control Desk installation provisioning controls | ControlDesk/ControlCloud/Frontend | First-manager setup-token issue wired | Client desk Cloud tab can save the local deployment profile, create a Control Cloud setup token, create a bootstrap package/install command, show generated token/package details, and issue/download a signed first-manager setup token through a layered Control Desk provisioning proxy |
| Control Desk installation history visibility | ControlDesk/ControlCloud/Frontend/Audit | Basic done | Client desk Cloud tab can refresh recent setup/bootstrap/registration/diagnostics/renewal audit events for the selected installation through a layered Control Desk audit proxy |
| Control Desk installation diagnostics visibility | ControlDesk/ControlCloud/Frontend/Diagnostics | Basic done | Client desk Cloud tab can refresh and download the latest diagnostics report for the selected installation through a layered Control Desk diagnostics proxy |
| Control Desk support command queue controls | ControlDesk/ControlCloud/Frontend/LocalServer | Basic done | Client desk Cloud tab can queue whitelisted `request_diagnostics` and `refresh_entitlement` support commands through a layered Control Desk proxy into Control Cloud's signed installation command queue; local-server command execution now processes both command types and acknowledges outcomes |
| SafarSuite runtime integration boundary | ControlCloud/LocalServer/SafarSuiteApp | App-runtime activation proof passed | Bootstrap packages now include a signed runtime plan, diagnostics endpoint, `runtime-services.manifest.json`, optional `safarsuite-app` Compose profile slot, shared module-gateway contract, runnable local-server API host, runtime diagnostics probing/log tail, service manifest-intent details, the placeholder app runtime probe in `tools\SafarSuite.AppRuntimeProbe`, and the app-workspace handoff at `docs/planning/safarsuite-app-integration-handoff.md`; the real app workspace now verifies v1 module-gateway menu/window access, backend write enforcement, reporting execution/audit enforcement, Windows client build, local-server publish, runtime Compose config, internal healthcheck, pushed image `ghcr.io/danionwheels/localserver:0.1.0`, runtime profile endpoint, profile-enriched health response, module-gateway logs with deployment/site context, pre-login activation import/export, pre-login first-manager setup, signed setup-token import/binding diagnostics, and activation revocation enforcement through the provider LocalServer LAN authority; the Control Cloud bootstrap app slot now matches the verified pushed app image/port/health/profile env contract, `tools\SafarSuite.LocalServer.EntitlementSmoke` guards the generated Compose/env/manifest artifact contract, `tools\SafarSuite.LocalServer.ComposeBootstrapProof` proves LocalServer bootstrap import, entitlement pull, heartbeat, module-gateway access, Control Cloud status readback, app-runtime profile boot, and app activation-token import through a generated package from the real Control Cloud API; `tools\SafarSuite.ControlCloud.PostgresProof` proves the same cloud receiver/bootstrap/registration/entitlement/heartbeat/command/status path through PostgreSQL-backed EF stores; app activation token issuance now lives in Control Cloud, requires the provider access gate, derives module grants from the latest signed entitlement bundle issue, records structured issue metadata in a durable searchable register, supports provider-gated revoke with audit, links replacement issuance to a revoked issue, queues signed revocation commands to LocalServer, exposes a local revocation-status endpoint for app enforcement, and is exposed through Control Desk Cloud tab request-import, issuance/download, provider-installation -> app-server mapping controls, activation history search, per-issue revoke, and prepare-replacement action. The app now centralizes outbound Local API CA validation in a shared client factory, module-gateway access remains hosted locally by the app LocalServer, and generated deployment artifacts default the Local API lane to HTTPS with generated local CA material. Remaining work is real provider-user authorization beyond the local provider key, production key storage/rotation, and clean customer setup UX |
| SafarSuite app LocalServer pairing flow | LocalServer/SafarSuiteApp/ControlCloud | Client Portal pairing status visibility wired | `docs/planning/safarsuite-app-local-server-pairing-flow.md` defines discovery as untrusted candidate gathering, trust through signed descriptors/pins/manager fingerprint confirmation, one-time provider app activation separate from local device pairing, first-manager setup-token import, manager-owned device/user rights, durable pairing profiles in protected client storage, cloud-assisted and offline-assisted modes, draft LocalServer pairing endpoints, and reconnect rules that prevent repeated client-side setup after a device is approved. Shared contracts, Control Cloud/Control Desk first-manager setup-token issue/download, LocalServer discovery/hello endpoints, file-backed pending device storage, manager approve/suspend/revoke endpoints, first-manager signed-token verification/import/consume, local manager bearer sessions, signed compact device credentials, credential verification endpoint, pairing config placeholders, generated-state persistence, heartbeat/status pairing snapshots, Control Desk and Client Portal pairing status display, and Compose/smoke/proxy/PostgreSQL proof assertions now exist; pending work is Windows app UX/storage in the SafarSuite app workspace. |
| Local module gateway contract | LocalServer/SafarSuiteApp/Entitlements | App workspace verified | Shared `LocalServerModuleAccessRequest`/`LocalServerModuleAccessResponse` contracts, local API endpoints, app workspace GET/POST v1 routes, app client access-policy consumption, and backend write-policy enforcement expose module access evaluation through the local module gateway |
| Local server API host | LocalServer/ControlCloud/SafarSuiteApp | Real Control Cloud proof passed | `SafarSuite.LocalServer.Api` boots with bootstrap import/status, entitlement pull, heartbeat, command processing, module access endpoints, file-backed local stores, role-aware runtime automation flags, and Docker command shims that keep API automation disabled, let worker handle entitlement pull/heartbeat, and let agent handle command polling; a generated Compose package has started the split API/worker/agent roles and completed signed bootstrap import, entitlement pull, heartbeat, and module-gateway access against both a stub cloud and a disposable real Control Cloud API |
| Deployment topology vocabulary | Contracts/ControlCloud/LocalServer | Basic done | Shared constants now distinguish bootstrap modes (`OnlineBootstrap`, `OfflineAssistedBootstrap`), client deployment modes (`OfflineLocal`, `BranchToHqSync`, `CloudSyncMultiBranch`, `HostedSaas`), and control versus operational data planes |
| Installation/deployment profile | ControlCloud/LocalServer/Contracts/Diagnostics | Basic done | Setup tokens and registered installations persist bootstrap mode, client deployment mode, site id, site role, optional parent site, branch code, and sync topology id; bootstrap package, signed bundle, registration, status, heartbeat, diagnostics, install command, and env template surfaces can carry the profile |
| Control Cloud setup/bootstrap audit visibility | ControlCloud/Audit | Basic done | Setup-token creation, bootstrap package generation, and local-server registration accept/reject events are recorded and exposed through `GET /api/v1/control-cloud/audit-events` |
| Local server diagnostics export/upload | LocalServer/ControlCloud/Audit | Runtime manifest intent added | Local-server libraries can export cached entitlement/trust-state diagnostics plus local import audit, runtime/build/channel, Docker/Compose, bootstrap checksum/status, service-state, and recent-error facts, upload to `POST /api/v1/local-server/installations/{installationId}/diagnostics`, and Control Cloud persists/latest-reads diagnostic reports; command-triggered diagnostics now fill runtime/bootstrap/service facts from the signed bootstrap runtime manifest, probe Docker/Compose service state, include each service's role/profile/image-env/port-env/internal-url/health-url/dependency intent, and collect recent warning/error log-tail lines when callers do not provide explicit facts |
| Paid module composition | Contracts/Entitlements/LocalServer/Billing | Basic catalog boundary done | Client contracts and entitlement snapshots require at least one enabled module; a flexible product module catalog can identify IncludedForAll versus PaidAddOn modules; module-backed billing rules and invoice draft lines retain the module code; deployed app access remains controlled by enabled module entries in the signed entitlement bundle |
| Local server entitlement verification | LocalServer/Entitlements | Basic done | New `SafarSuite.LocalServer` layers can pull the latest signed bundle from Control Cloud, import offline renewal files that wrap signed bundles, verify HMAC signatures, reject bad signatures, older versions, and same-file replay, persist trust state for last accepted bundle/last trusted cloud time/clock rollback warnings, persist local import audit records, cache the latest accepted bundle, gate module access through active/warning/grace/restricted/expired states, and report heartbeat/license status |
| Local server import audit | LocalServer/Entitlements/Diagnostics | Basic done | Accepted/rejected Control Cloud pulls, direct bundle imports, and offline renewal-file imports are stored locally with source, result, version, bundle issue, failure code, payload hash, and signing key id; diagnostics includes the recent records |
| Payment review and reversal foundation | Payments/Accounting/Frontend | Basic done | Bank transfers enter pending review without GL posting; approval posts receipt journal and payment events; rejection closes pending review; reversal posts a dedicated reversal journal and updates the client statement |
| Billing tax foundation | Billing/Accounting/Frontend/Persistence | Basic done | Client charge rules support tax percent, invoice drafts materialize tax lines, invoice issue credits tax payable, and EF migration adds tax/line-type persistence |
| Unpaid invoice void foundation | Billing/Accounting/ControlCloud/Frontend | Basic done | Unpaid issued invoices can be voided with a reversing GL journal, `InvoiceVoided` outbox event, and statement visibility; paid invoice correction remains a later credit/refund workflow |
| Full credit note foundation | Billing/Accounting/ControlCloud/Frontend/Persistence | Basic done | Paid and partially paid invoices can receive one full credit note with reversing sale journal, `CreditNoteIssued` outbox event, persistence, and statement credit visibility |
| Client refund foundation | Payments/Accounting/ControlCloud/Frontend/Persistence | Basic done | Client credit balances can be refunded with a balanced AR/cash-bank journal, `ClientRefundIssued` outbox event, persistence, and statement visibility |
| Client credit settlement foundation | Payments/Billing/ControlCloud/Frontend/Persistence | Basic done | Unapplied client credit can be allocated to issued/partially paid invoices without a new GL journal, with `ClientCreditApplied` outbox event, persistence, invoice balance updates, and statement visibility |
| Accounting action UI guardrails | Billing/Payments/Frontend | Basic done | Client desk disables incomplete invoice/payment/credit/refund actions and asks for confirmation before GL-impacting issue, void, credit note, receipt, approval, reversal, settlement, and refund actions |
| Accounting chain smoke runner | Tools/Accounting | GL close handoff done | `tools/SafarSuite.ControlDesk.AccountingSmoke` exercises the core local chain through application handlers and verifies controlled COA setup, repair-plan zero-action checks for clean setup, invalid ledger-code refusals, posting/parent guard refusals, balanced journals, statement credit behavior, outbox events, default accounting controls, reports, and retained-earnings close handoff; both in-memory and PostgreSQL modes have passed historically, with the latest in-memory run passing on 2026-07-04 |

## Parking Lot

| Item | Reason Parked | Status |
| --- | --- | --- |
| Full Survey/FAS form cloning | Too broad for Milestone 1 | Deferred |
| Control Desk UI bundle code-splitting | Vite production build passes, but the main JS chunk is over the default 500 kB warning threshold; split heavy module areas later when frontend flow stabilizes | Proposed |
| SurveyValuation module | No longer part of the product goal for SafarSuite Control Desk; code/docs remain as reference but API routes and DI registrations are not active | Deferred |
| Legacy form clone plan | Historical reference only after pivot away from Survey/FAS clone | Deferred |
| Legacy form name map | Historical reference only after pivot away from Survey/FAS clone | Deferred |
| Survey Job Entry implementation | Work exists but is paused; do not extend unless requirements change | Deferred |
| Tauri desktop packaging | Wait until browser-hosted React UI proves the first flow | Deferred |
| Payment gateway selection | Needs business decision | Proposed |
| Client business-data sync data plane | Future multi-branch/HQ/cloud operational sync beyond the current commercial control, entitlement, heartbeat, diagnostics, and deployment work | Proposed |
| SQL backup restore | Useful later for sample data and parity checks | Proposed |
| PostgreSQL persistence for Survey Job Entry | Not needed while SurveyValuation remains parked | Deferred |
| Broader Survey reference lookups | Not needed while SurveyValuation remains parked | Deferred |
