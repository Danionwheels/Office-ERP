# SafarSuite Control Desk UI

React + TypeScript frontend for the SafarSuite Control Desk desktop experience.

Run the backend API first on `http://127.0.0.1:5188`, then run:

```text
npm run dev
```

Current first screen:

- Client maintenance desk under `src/modules/clients`
